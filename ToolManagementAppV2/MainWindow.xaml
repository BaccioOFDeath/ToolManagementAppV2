<Window x:Class="ToolManagementAppV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tool Management" Width="1280" Height="800" WindowStartupLocation="CenterScreen" Background="#0F141A">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="Button" x:Key="NavButton">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#E6F0F5"/>
            <Setter Property="Background" Value="#1B2430"/>
            <Setter Property="BorderBrush" Value="#2A3542"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#243142"/>
                                <Setter Property="BorderBrush" Value="#3A4B5E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E2936"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#6C7A86"/>
                                <Setter Property="Background" Value="#17202A"/>
                                <Setter Property="BorderBrush" Value="#232E3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        <Style TargetType="Frame">
            <Setter Property="NavigationUIVisibility" Value="Hidden"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="ContentControl">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Left" Width="260" Background="#111820" BorderBrush="#1E2A36" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Margin="16,16,16,8">
                    <TextBlock Text="Tool Management" FontSize="18" Foreground="#E6F0F5" FontWeight="SemiBold"/>
                    <TextBlock Text="Dashboard" FontSize="12" Foreground="#93A4B3" Margin="0,4,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1">
                    <StackPanel x:Name="LeftMenuStack" Orientation="Vertical" Margin="12,0,12,8">
                        <Button Style="{StaticResource NavButton}" Content="Dashboard" Command="{Binding OpenDashboardCommand}"/>
                        <Button Style="{StaticResource NavButton}" Content="Search Tools" Command="{Binding OpenSearchToolsCommand}"/>

                        <Button Style="{StaticResource NavButton}" Content="Tool Management" Command="{Binding OpenToolManagementCommand}" Visibility="{Binding IsCurrentUserAdmin, Converter={StaticResource BoolToVis}}"/>
                        <Button Style="{StaticResource NavButton}" Content="Rentals" Command="{Binding OpenRentalsCommand}"/>
                        <Button Style="{StaticResource NavButton}" Content="Users" Command="{Binding OpenUsersCommand}" Visibility="{Binding IsCurrentUserAdmin, Converter={StaticResource BoolToVis}}"/>
                        <Button Style="{StaticResource NavButton}" Content="Customers" Command="{Binding OpenCustomersCommand}"/>
                        <Button Style="{StaticResource NavButton}" Content="Import/Export" Command="{Binding OpenImportExportCommand}" Visibility="{Binding IsCurrentUserAdmin, Converter={StaticResource BoolToVis}}"/>
                        <Button Style="{StaticResource NavButton}" Content="Settings" Command="{Binding OpenSettingsCommand}" Visibility="{Binding IsCurrentUserAdmin, Converter={StaticResource BoolToVis}}"/>
                        <Button Style="{StaticResource NavButton}" Content="Activity Logs" Command="{Binding OpenActivityLogsCommand}"/>
                        <Button Style="{StaticResource NavButton}" Content="Reports" Command="{Binding OpenReportsCommand}"/>

                        <Separator Margin="6,12"/>
                        <Button Style="{StaticResource NavButton}" Content="Exit" Command="{Binding ExitCommand}"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="12,8,12,16">
                    <Ellipse Width="10" Height="10" Fill="#2ECC71" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUserName}" Margin="8,0,0,0" Foreground="#CFE3F0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUserRole}" Margin="6,0,0,0" Foreground="#93A4B3" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="#0F141A" Padding="16,12" BorderBrush="#1E2A36" BorderThickness="0,0,0,1">
                <DockPanel>
                    <TextBlock Text="{Binding CurrentPageTitle}" DockPanel.Dock="Left" FontSize="20" Foreground="#E6F0F5" FontWeight="SemiBold"/>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Margin="8">
                        <TextBox Width="260" Height="34" VerticalAlignment="Center" Background="#141C24" Foreground="#E6F0F5" BorderBrush="#2A3542" BorderThickness="1" Padding="10,4" Text="{Binding GlobalSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Style="{StaticResource NavButton}" Width="120" Content="Search" Command="{Binding GlobalSearchCommand}"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="#0F141A" Padding="16">
                <ContentControl Content="{Binding CurrentPage}"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
