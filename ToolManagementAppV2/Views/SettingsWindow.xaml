<!-- Views/SettingsWindow.xaml -->
<Window x:Class="ToolManagementAppV2.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="900" Height="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <DockPanel Margin="10">
        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushAlt}" BorderThickness="1" Padding="10" CornerRadius="8">
            <TextBlock Text="Settings" FontWeight="SemiBold" FontSize="18"/>
        </Border>
        <Border Style="{StaticResource Card}" Padding="0" Margin="0,8,0,0">
            <Frame NavigationUIVisibility="Hidden" Content="{Binding SettingsPageContent}"/>
        </Border>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Style="{StaticResource PrimaryButton}" Content="Save" Command="{Binding SaveSettingsCommand}"/>
            <Button Style="{StaticResource PrimaryButton}" Content="Close" Command="{Binding CloseCommand}" Margin="8,0,0,0"/>
        </StackPanel>
    </DockPanel>
</Window>
