<!-- Views/UsersEditWindow.xaml -->
<Window x:Class="ToolManagementAppV2.Views.UsersEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:ToolManagementAppV2.Utilities.Converters"
        Title="Edit User" Width="520" Height="520" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <conv:NullToDefaultImageConverter x:Key="NullToDefaultImageConverter"/>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.RowSpan="7" Width="96" Height="96" CornerRadius="8" Background="{StaticResource SurfaceAltBrush}" Margin="0,0,12,0">
                <Image Width="96" Height="96"
                       Source="{Binding EditingUser.UserPhotoPath, Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=user}"
                       Stretch="UniformToFill" ClipToBounds="True"/>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Style="{StaticResource PrimaryButton}" Content="Browse Avatar" Command="{Binding BrowseAvatarCommand}" Margin="0,0,8,0"/>
                <Button Style="{StaticResource PrimaryButton}" Content="Remove Avatar" Command="{Binding RemoveAvatarCommand}"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Username" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EditingUser.UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Email" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EditingUser.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Phone" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding EditingUser.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Mobile" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding EditingUser.Mobile, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Role" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <ComboBox Grid.Row="5" Grid.Column="1" SelectedValuePath="Tag" SelectedValue="{Binding EditingUser.IsAdmin, Mode=TwoWay}" Margin="0,0,0,8">
                <ComboBoxItem Content="User" Tag="False"/>
                <ComboBoxItem Content="Admin" Tag="True"/>
            </ComboBox>

            <CheckBox Grid.Row="6" Grid.Column="1" Content="Active" IsChecked="{Binding EditingUser.IsActive, Mode=TwoWay}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource PrimaryButton}" Content="Save" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
            <Button Style="{StaticResource PrimaryButton}" Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
