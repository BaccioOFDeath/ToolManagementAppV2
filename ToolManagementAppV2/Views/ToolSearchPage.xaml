<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      x:Class="ToolManagementAppV2.Views.ToolSearchPage"
      Loaded="Page_Loaded"
      SizeChanged="Page_SizeChanged"
      Background="{DynamicResource BackgroundBrush}">
    <Page.Resources>
        <DataTemplate x:Key="ToolCardTemplate">
            <Grid Margin="5"
                  Background="{DynamicResource CardBackgroundBrush}"
                  Cursor="Hand"
                  Focusable="True"
                  Opacity="0">
                <Grid.RenderTransform>
                    <ScaleTransform x:Name="TileScale" ScaleX="1" ScaleY="1"/>
                </Grid.RenderTransform>
                <Grid.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 From="0.9" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 From="0.9" To="1" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="UIElement.GotFocus">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1.05" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1.05" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="UIElement.LostFocus">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>
                <Grid>
                    <Image Source="{Binding ToolImagePath}" Stretch="UniformToFill"/>
                    <Border Background="{DynamicResource OverlayBrush}" VerticalAlignment="Bottom" Height="40">
                        <TextBlock Text="{Binding NameDescription}"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>
            </Grid>
        </DataTemplate>

        <Style x:Key="NarrowTileStyle" TargetType="ContentPresenter">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="220"/>
        </Style>
        <Style x:Key="WideTileStyle" TargetType="ContentPresenter">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="300"/>
        </Style>
    </Page.Resources>

    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="WidthStates">
            <VisualState x:Name="Narrow">
                <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HandToolsList" Storyboard.TargetProperty="ItemContainerStyle">
                        <DiscreteObjectKeyFrame Value="{StaticResource NarrowTileStyle}" KeyTime="0:0:0" />
                    </ObjectAnimationUsingKeyFrames>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PowerToolsList" Storyboard.TargetProperty="ItemContainerStyle">
                        <DiscreteObjectKeyFrame Value="{StaticResource NarrowTileStyle}" KeyTime="0:0:0" />
                    </ObjectAnimationUsingKeyFrames>
                    <DoubleAnimation Storyboard.TargetName="HandHeader" Storyboard.TargetProperty="FontSize" To="18" Duration="0" />
                    <DoubleAnimation Storyboard.TargetName="PowerHeader" Storyboard.TargetProperty="FontSize" To="18" Duration="0" />
                </Storyboard>
            </VisualState>

            <VisualState x:Name="Wide">
                <Storyboard>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HandToolsList" Storyboard.TargetProperty="ItemContainerStyle">
                        <DiscreteObjectKeyFrame Value="{StaticResource WideTileStyle}" KeyTime="0:0:0" />
                    </ObjectAnimationUsingKeyFrames>
                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PowerToolsList" Storyboard.TargetProperty="ItemContainerStyle">
                        <DiscreteObjectKeyFrame Value="{StaticResource WideTileStyle}" KeyTime="0:0:0" />
                    </ObjectAnimationUsingKeyFrames>
                    <DoubleAnimation Storyboard.TargetName="HandHeader" Storyboard.TargetProperty="FontSize" To="24" Duration="0" />
                    <DoubleAnimation Storyboard.TargetName="PowerHeader" Storyboard.TargetProperty="FontSize" To="24" Duration="0" />
                </Storyboard>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock x:Name="HandHeader" Text="Hand Tools" FontSize="24" Margin="10,0,10,5"/>
            <ItemsControl x:Name="HandToolsList" ItemsSource="{Binding HandTools}"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                          ScrollViewer.CanContentScroll="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="ToolCardTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="PowerHeader" Text="Power Tools" FontSize="24" Margin="10,20,10,5"/>
            <ItemsControl x:Name="PowerToolsList" ItemsSource="{Binding PowerTools}"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                          ScrollViewer.CanContentScroll="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="ToolCardTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
