<!-- Views/ImportExportPage.xaml -->
<Page x:Class="ToolManagementAppV2.Views.ImportExportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}">
    <Page.Resources>
        <Style x:Key="CompactPrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </Page.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Tools -->
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Tools" Style="{StaticResource SectionHeader}" />
                <WrapPanel>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Import Tools CSV" Command="{Binding ImportToolsCommand}"/>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Import Tool Images" Command="{Binding OpenImageImportMappingWindowCommand}"
                            Visibility="{Binding IsCurrentUserAdmin, Converter={StaticResource BoolToVis}}"/>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Export Tools CSV" Command="{Binding ExportToolsCommand}"/>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Backup Database" Command="{Binding BackupDatabaseCommand}"/>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Cancel Import" Command="{Binding CancelImportToolsCommand}" IsEnabled="{Binding ImportToolsCommand.IsRunning}"/>

                </WrapPanel>
            </StackPanel>

            <!-- Customers -->
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Text="Customers" Style="{StaticResource SectionHeader}" />
                <WrapPanel>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Import Customers CSV" Command="{Binding ImportCustomersCommand}"/>
                    <Button Style="{StaticResource CompactPrimaryButton}" Content="Export Customers CSV" Command="{Binding ExportCustomersCommand}"/>
                </WrapPanel>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ImportExportLogs}" IsReadOnly="True" AutoGenerateColumns="False"
                  Style="{StaticResource VirtualizedDataGridStyle}" Margin="0,8,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Message" Binding="{Binding}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
