<!-- Views/DeviceSettingsPage.xaml -->
<Page x:Class="DeviceManagementApp.Views.Pages.DeviceSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundBrush}">

    <ScrollViewer>
        <StackPanel Margin="8" Orientation="Vertical">
            <Border Style="{StaticResource Card}" Padding="12">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Device Discovery" Style="{StaticResource SectionHeader}"/>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Subnets" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Subnets}" Margin="8,0,0,0"/>

                        <TextBlock Grid.Row="1" Text="FTP Ports" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FtpPorts}" Margin="8,0,0,0" PreviewTextInput="PortsBox_PreviewTextInput"/>

                        <TextBlock Grid.Row="2" Text="Additional Ports" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding AdditionalPorts}" Margin="8,0,0,0" PreviewTextInput="AdditionalPortsBox_PreviewTextInput"/>

                        <TextBlock Grid.Row="3" Text="Liveness Timeout (ms)" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding LivenessTimeoutMs}" Margin="8,0,0,0" PreviewTextInput="TimeoutBox_PreviewTextInput"/>

                        <TextBlock Grid.Row="4" Text="Port Probe Timeout (ms)" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PortProbeTimeoutMs}" Margin="8,0,0,0" PreviewTextInput="TimeoutBox_PreviewTextInput"/>

                        <Button x:Name="SaveButton" Grid.Row="5" Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="Save" Command="{Binding SaveCommand}" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
