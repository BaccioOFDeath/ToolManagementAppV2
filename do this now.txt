Use this as the complete agent prompt:

```text
Repository: BaccioOFDeath/ToolManagementAppV2

Mission:
Continue making InventoryManagementApp boringly excellent: stable, complete, professional, predictable, safe, and ready for real workshop use.

The target standard is not “mostly working”. The target standard is a complete desktop application where a normal user can open the app, manage inventory, rent items out, check items back in, manage customers, print documents, import/export data, configure settings, and recover from errors without confusion, data loss, or broken buttons.

Work only in:
- InventoryManagementApp
- InventoryManagementApp.Tests
- repository docs directly related to InventoryManagementApp progress, QA, deployment, testing, or agent instructions

Do not touch:
- backend services
- web apps
- mobile apps
- device-management systems
- unrelated projects
- generated binaries
- local databases
- logs
- bin
- obj
- machine-specific files

Do not add another ORM.
Use the existing SQLite DatabaseService/repository patterns.
Do not commit secrets.

Read first:
1. AGENTS.md
2. README.md
3. designprompt.md
4. HOURLY_APP_QA_AGENT_PROMPT.md if present
5. InventoryManagementApp/ProgressNotes/APP_COMPLETION_CHECKLIST.md if present
6. InventoryManagementApp/ProgressNotes/2026-06-16-audit.md if present

Operating loop:
1. Pull latest master.
2. Check current repository status.
3. Run validation before work:
   - dotnet restore InventoryManagementApp.sln
   - dotnet build InventoryManagementApp.sln --no-restore
   - dotnet test InventoryManagementApp.sln --no-build
   - ./scripts/check-banned-words.sh if available
4. If validation is already failing, fix validation before feature work unless the failure is clearly unrelated and documented.
5. Pick one concrete high-value weakness.
6. Implement the full slice end to end.
7. Add or update tests for every behavior change.
8. Re-run validation.
9. Commit only complete, focused work.
10. Push the branch/change.
11. Open a PR if working on a branch.
12. Merge according to the aggressive auto-merge policy below.
13. Update progress notes.

Current priority:
Harden item CSV import safety in InventoryManagementApp/Services/Items/ItemService.cs.

Goal:
Item imports must never leave a partially corrupted batch. Bad rows and duplicates should be skipped and reported as before, but unexpected import failures during writes must roll back all rows inserted by that import attempt.

Requirements:
- Use the existing SQLite DatabaseService/repository patterns.
- Wrap item CSV import writes in an explicit transaction where appropriate.
- Preserve current invalid/skipped row behavior.
- Preserve current duplicate/skipped row behavior.
- Do not change public import formats unless required for safety.
- Do not hide errors.
- Add tests in InventoryManagementApp.Tests for:
  - valid rows import successfully
  - invalid rows are reported/skipped
  - duplicate item numbers are skipped
  - unexpected insert failure rolls back prior inserted rows
- Keep the change focused and reversible.

If this priority is already fixed or blocked:
Choose the next highest-risk item from InventoryManagementApp/ProgressNotes/2026-06-16-audit.md.

If that file is missing, create or update:
InventoryManagementApp/ProgressNotes/APP_COMPLETION_CHECKLIST.md

Prefer this priority order:
1. App cannot build, test, or start.
2. Data-loss or corruption risk.
3. Checkout/rent/check-in broken.
4. Rental request confirm/cancel broken.
5. Item/customer CRUD broken.
6. Import/export unsafe or incomplete.
7. Printing broken, hard-coded, or not professional.
8. Email config unsafe, unclear, or silently failing.
9. Missing tests around critical workflows.
10. Weak error handling or silent failures.
11. UI professionalism and workflow polish.
12. Documentation drift.

Core app completion targets:
The app is not complete until these workflows are professional and tested where practical:

Inventory:
- Add item.
- Edit item.
- Delete one item.
- Delete multiple items.
- Search items.
- View item details.
- Display item image/default image.
- Persist rental eligibility, quantity, location, supplier, notes, keywords, powered state, and item numbers.
- Prevent invalid item data from being saved silently.

Rental checkout:
- Rent item to a customer.
- Add customer from rental dialog.
- Confirm Rental button enables immediately after valid customer selection.
- Due date persists.
- Cancel leaves item unchanged.
- Successful rental updates availability and visible checked-out state.
- User sees a useful success/error state.

Rental management:
- Search/filter rentals.
- Check in active rental.
- Extend due date.
- Open rental details.
- Open rental history.
- Delete rental with confirmation.
- Refresh state after actions.
- Show active, returned, and overdue rentals correctly.

Rental requests/reservations:
- Place request for unavailable item.
- Show open requests.
- Confirm request.
- Cancel request.
- Print request.
- Print request queue.
- Current holder and next action text must match selected request.
- Status changes must persist and refresh.

Customers:
- Add customer.
- Edit customer.
- Delete customer.
- Search/filter customer.
- Use customer details correctly in rentals, invoices, reminders, and print documents.
- Validate required fields.

Printing:
- Print Rental.
- Print Picking Slip.
- Print Invoice.
- Print Search Results.
- Print Checked Out.
- Print Requests.
- Print labels.
- Preview must open.
- Preview must contain selected record data.
- Print button must open the Windows print dialog.
- Cancel from print dialog must not crash.
- Printer exceptions must be caught and shown professionally.
- Company details, logo, rental rates, and fees should come from settings, not hard-coded placeholders, where practical.

Import/export:
- Import CSV.
- Import JSON.
- Import XML.
- Export CSV.
- Export JSON.
- Export XML.
- Bad files must show useful row-level errors.
- Bad imports must not corrupt existing data.
- Duplicate records must be skipped/reported.
- Mapping dialogs must validate required fields.

Settings/admin:
- App name updates visible UI.
- Item singular/plural labels update visible UI.
- Company details affect documents.
- Logo path works.
- Invalid logo path degrades gracefully.
- Rental rates/fees affect invoices.
- Email settings validate without exposing secrets.
- Theme/display settings persist.
- Auto logout does not interrupt active modal workflows unexpectedly.
- Admin-only sections remain admin-only.

Reports/activity:
- Reports page opens.
- Reports produce useful output or a clear empty state.
- Activity logs load.
- Recent user actions appear in activity logs where expected.

Maintenance/calibration/reservations/kits/categories:
- Page opens.
- Add record.
- Edit record.
- Delete/cancel record.
- Search/filter if present.
- Empty state is professional.
- Save persists after reload.

UI standard:
If any XAML is touched, follow designprompt.md exactly:
- dense
- native
- compact
- clear
- keyboard-friendly
- table-focused
- practical WPF
- obvious workflows
- restrained styling
- strong labels
- clear section titles
- tight spacing
- actions close to the data they affect

Do not use:
- trendy web styling
- oversized cards
- mobile spacing
- decorative empty space
- excessive rounded corners
- unnecessary custom controls
- hidden actions
- ambiguous icons

Screenshot requirement:
If UI is touched, run the app and capture before/after screenshots of every changed screen/dialog.

Save screenshots locally under:
.qa-screenshots/YYYY-MM-DD-HH/

Do not commit screenshots unless explicitly requested.

If screenshots cannot be captured, document the exact blocker.

Testing standard:
For every behavior change, add or update tests where practical.

Prefer tests for:
- command enable/disable behavior
- validation
- persistence
- transaction rollback
- duplicate handling
- import/export edge cases
- rental checkout/check-in
- request confirm/cancel
- print document generation
- settings-driven behavior
- error handling

Do not skip tests just because the change is small if the bug could return.

Validation commands:
Run these before final commit/merge:
- dotnet restore InventoryManagementApp.sln
- dotnet build InventoryManagementApp.sln --no-restore
- dotnet test InventoryManagementApp.sln --no-build
- ./scripts/check-banned-words.sh if available

If validation cannot run:
- document the exact command
- document the exact error
- state whether the blocker is environmental or caused by the change
- do not pretend validation passed

Completion checklist:
Maintain:
InventoryManagementApp/ProgressNotes/APP_COMPLETION_CHECKLIST.md

It must include:
- completed workflows
- known broken workflows
- partially complete workflows
- next recommended target
- validation status
- date/time of last audit

Progress notes:
For every run, update or create a progress note under:
InventoryManagementApp/ProgressNotes/

Use a clear date-based name when appropriate.

Include:
- what was inspected
- what was broken
- what was fixed
- files changed
- tests added or updated by the completed change only
- validation result
- merge status
- next target

Aggressive auto-merge policy:
Auto-commit, push, open PRs, and merge completed focused work without waiting for human review whenever the change is internally consistent and there is no clear danger.

Merge automatically when:
- work is scoped to InventoryManagementApp, InventoryManagementApp.Tests, or related progress/docs
- the diff is focused and understandable
- no secrets, databases, logs, bin, obj, generated binaries, or machine-specific files are included
- validation passes, OR validation cannot run for an environment reason that is clearly documented and the change is low-risk
- tests were added/updated for behavior changes where practical
- the app still builds logically from code inspection if build cannot run
- the PR is mergeable
- there is no clear unsafe signal

Allow auto-merge even for:
- medium-size fixes
- database safety fixes with tests
- printing fixes
- rental/check-in/checkout fixes
- import/export hardening
- UI polish
- missing validation
- missing error handling
- missing tests
- focused refactors that directly support the fix

Do not auto-merge only when:
- the change touches secrets, auth, permissions, encryption, or credential handling
- the change introduces a new database engine, backend service, web/mobile app, queue, or unrelated architecture
- the change deletes large sections of working functionality
- validation fails for a reason caused by the change
- there is an unresolved merge conflict
- the agent is unsure whether data could be lost or corrupted
- the diff includes generated binaries, local DBs, logs, bin, obj, or machine-specific files
- the change is broad and not tied to one clear user-visible outcome

If validation cannot run:
- still commit and push if the code change is focused and reviewable
- open the PR
- auto-merge only if the risk is low and the blocker is environmental
- otherwise leave PR open and explain the blocker

Preferred merge method:
- squash merge for PRs
- direct commit to master is acceptable only for small, obvious documentation, test, prompt, or single-file fixes

Auto-merge stance:
Be aggressive. The default is to merge focused completed improvements. Do not stall waiting for perfect certainty. Only stop when there is a concrete unsafe signal, a validation failure caused by the change, or a broad/high-risk diff.

Final merge rule:
Default to merging completed focused improvements. Do not leave work unmerged unless there is a specific reason from the “Do not auto-merge” list.

Commit rules:
- Commit only complete work.
- Use clear commit messages.
- Keep commits focused.
- Do not mix unrelated fixes.
- Do not commit local screenshots unless explicitly requested.
- Do not commit secrets, local databases, logs, bin, obj, or generated binaries.

Final response:
Use this exact format:

Weakness fixed:
- <summary>

Files changed:
- <file>
- <file>

Tests added/updated:
- <test>
- <test>
  Only include tests that were added or modified by the completed change. Do not list unrelated pre-existing failing tests.

Validation:
- restore: <passed/failed/not run + reason>
- build: <passed/failed/not run + reason>
- test: <passed/failed/not run + reason>
- banned words: <passed/failed/not run + reason>
  If a downstream command was not executed because an earlier validation step failed, report it as `not run` rather than as a failed test.

Commit/PR/merge:
- commit: <sha/message>
- PR: <number/link/status or not used>
- merge: <merged/not merged + reason>

Screenshots:
- <folder path or not applicable/blocker>

Blockers:
- <none or exact blocker>

Next recommended target:
- <single next action>
```
