<!-- Resources/Templates.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:InventoryManagementApp.Utilities.Helpers"
                    xmlns:models="clr-namespace:InventoryManagementApp.Models">
    <DataTemplate x:Key="StatCardTemplate">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Title}" Style="{StaticResource LabelTextBlock}"/>
                <TextBlock Grid.Row="1" Text="{Binding Value}" Style="{StaticResource StatisticValueTextBlock}"/>
            </Grid>
        </Border>
    </DataTemplate>
    <ItemsPanelTemplate x:Key="WrapPanelItems">
        <WrapPanel Orientation="Horizontal" ItemWidth="260" ItemHeight="100"/>
    </ItemsPanelTemplate>
    <DataTemplate x:Key="ItemCardTemplate">
        <Border Style="{StaticResource Card}" Width="220" Padding="0">
            <Border.Visibility>
                <MultiBinding Converter="{StaticResource AnyTrueToVisibilityConverter}">
                    <Binding Path="DataContext.ShowImage" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowName" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowItemNumber" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowBrand" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowLocation" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowPrice" RelativeSource="{RelativeSource AncestorType=Page}"/>
                    <Binding Path="DataContext.ShowNotes" RelativeSource="{RelativeSource AncestorType=Page}"/>
                </MultiBinding>
            </Border.Visibility>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="140"/>
                    <RowDefinition Height="120"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushAlt}" BorderThickness="1" CornerRadius="8" Visibility="{Binding DataContext.ShowImage, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}">
                    <Image Stretch="UniformToFill" Source="{Binding ImagePath, Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=item}"/>
                </Border>
                <Grid Grid.Row="1" Margin="0,6,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Name}" Style="{StaticResource ListItemTitleTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowName, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Row="1" Text="{Binding ItemNumber}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowItemNumber, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Row="2" Text="{Binding Brand}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowBrand, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Row="3" Text="{Binding Location}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowLocation, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Row="4" Text="{Binding Price, StringFormat={}{0:C}}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowPrice, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Row="5" Text="{Binding Notes}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxHeight="20" Visibility="{Binding DataContext.ShowNotes, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}"/>
                </Grid>
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                    <Button Content="Rent"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding DataContext.RentItemCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}"/>
                    <Button Command="{Binding DataContext.ToggleCheckOutCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}" Margin="6,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                <Setter Property="Content" Value="Check Out"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCheckedOut}" Value="True">
                                        <Setter Property="Content" Value="Check In"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="ActivityLogItemTemplate">
        <StackPanel>
            <TextBlock Text="{Binding Action}" Style="{StaticResource ListItemTitleTextBlock}"/>
            <TextBlock Style="{StaticResource CaptionTextBlock}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} - {1:yyyy-MM-dd HH:mm}">
                        <Binding Path="UserName"/>
                        <Binding Path="Timestamp" Converter="{StaticResource DateTimeMinToEmptyStringConverter}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </DataTemplate>

    <!-- Shared rental DataGrid columns -->
    <DataGridTextColumn x:Key="RentalIdColumn" Header="Rental #" Binding="{Binding RentalId}" Width="120" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalItemNumberColumn" Header="{Binding ItemLabelSingular, Source={x:Static helpers:LabelProvider.Instance}, StringFormat='{}{0} #'}" Binding="{Binding ItemNumber}" Width="120" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalItemNameColumn" Header="{Binding ItemLabelSingular, Source={x:Static helpers:LabelProvider.Instance}}" Binding="{Binding Name}" Width="*" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalCustomerColumn" Header="Customer" Binding="{Binding CustomerName}" Width="220" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalOutColumn" Header="Out" Binding="{Binding DateOut, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="180" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalDueColumn" Header="Due" Binding="{Binding DueDate, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="180" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalReturnedColumn" Header="Returned" Binding="{Binding DateReturned, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="180" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalStatusColumn" Header="Status" Binding="{Binding Status}" Width="140" x:Shared="False"/>
</ResourceDictionary>
