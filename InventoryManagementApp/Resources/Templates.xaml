<!-- Resources/Templates.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:InventoryManagementApp.Utilities.Helpers"
                    xmlns:models="clr-namespace:InventoryManagementApp.Models">
    <DataTemplate x:Key="StatCardTemplate">
        <Border Style="{StaticResource DesktopSummaryCard}" Padding="10,8" Margin="0,0,6,6" MinWidth="170">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource LabelTextBlock}"/>
                <TextBlock Text="{Binding Value}" Style="{StaticResource StatisticValueTextBlock}" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>
    </DataTemplate>
    <ItemsPanelTemplate x:Key="WrapPanelItems">
        <WrapPanel Orientation="Horizontal"/>
    </ItemsPanelTemplate>
    <DataTemplate x:Key="ItemCardTemplate">
        <Button Command="{Binding DataContext.OpenItemCardCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                CommandParameter="{Binding}"
                Background="Transparent"
                BorderBrush="Transparent"
                BorderThickness="0"
                Padding="0"
                Margin="0"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
            <Border Width="196"
                    Margin="3"
                    Padding="5"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderThickness="1,1,1,3">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRentedStock}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsCheckedOut}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding HasNoOnHand}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsIncomplete}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="96"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0"
                            Background="{DynamicResource SurfaceAltBrush}"
                            BorderBrush="{DynamicResource BorderBrushAlt}"
                            BorderThickness="1">
                        <Image Stretch="Uniform"
                               Margin="4"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Source="{Binding ImagePath, Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=item}"/>
                    </Border>
                    <Grid Grid.Row="1" Margin="0,5,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}" Style="{StaticResource ListItemTitleTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Row="1" Text="{Binding ItemNumber}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Grid>
            </Border>
        </Button>
    </DataTemplate>

    <DataTemplate x:Key="ActivityLogItemTemplate">
        <StackPanel>
            <TextBlock Text="{Binding Action}" Style="{StaticResource ListItemTitleTextBlock}"/>
            <TextBlock Style="{StaticResource CaptionTextBlock}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} - {1:yyyy-MM-dd HH:mm}">
                        <Binding Path="UserName"/>
                        <Binding Path="Timestamp" Converter="{StaticResource DateTimeMinToEmptyStringConverter}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </DataTemplate>

    <!-- Shared rental DataGrid columns -->
    <DataGridTextColumn x:Key="RentalIdColumn" Header="Rental #" Binding="{Binding RentalID}" Width="90" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalItemNumberColumn" Header="{Binding ItemLabelSingular, Source={x:Static helpers:LabelProvider.Instance}, StringFormat='{}{0} #'}" Binding="{Binding ItemNumber}" Width="115" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalItemNameColumn" Header="Location" Binding="{Binding ItemLocation}" Width="160" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalCustomerColumn" Header="Customer" Binding="{Binding CustomerName}" Width="200" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalOutColumn" Header="Out" Binding="{Binding RentalDate, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="150" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalDueColumn" Header="Due" Binding="{Binding DueDate, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="150" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalReturnedColumn" Header="Returned" Binding="{Binding ReturnDate, Converter={StaticResource DateTimeMinToEmptyStringConverter}, StringFormat=\{0:yyyy-MM-dd HH:mm\}, TargetNullValue=''}" Width="150" x:Shared="False"/>
    <DataGridTextColumn x:Key="RentalStatusColumn" Header="Status" Binding="{Binding Status}" Width="120" x:Shared="False"/>
</ResourceDictionary>
