# InventoryManagementApp Progress Audit - 2026-06-16

Scheduled run scope: `InventoryManagementApp` only.

Validation status: local validation is blocked in this scheduled container because `dotnet` is not installed and direct GitHub cloning is blocked by the network proxy. The code fix and test were committed through the connected GitHub API. WPF screenshot review was not possible for the same reason; this pass used static XAML, ViewModel, service, repository, and test inspection.

## Audit Table

| Area | Current state | Weakness | Risk | Recommended fix | Files involved | Tests needed | Status |
|---|---|---|---|---|---|---|---|
| MainWindow | Main window and navigation are present through `MainWindow.xaml`, `MainWindow.xaml.cs`, `MainViewModel`, and `Navigation`. | Full launch and screenshot pass could not run in this environment. | Broken navigation or weak initial focus could escape static review. | Run WPF launch on a Windows agent and capture every reachable route. | `InventoryManagementApp/MainWindow.xaml`, `InventoryManagementApp/MainWindow.xaml.cs`, `InventoryManagementApp/ViewModels/MainViewModel.cs`, `InventoryManagementApp/Utilities/Navigation.cs` | Startup/navigation smoke tests plus screenshot review. | Needs runtime UI review. |
| Dashboard | Dashboard page and tests are present. | Static-only review this pass; density and command reachability still need screenshot verification. | Dashboard can drift toward decorative or stale summary data. | Verify dashboard metrics against `designprompt.md`; keep tables and compact summary rows. | `Views/Pages/DashboardPage.xaml`, `Views/Pages/DashboardPage.xaml.cs`, `ViewModels/DashboardViewModel.cs`, `InventoryManagementApp.Tests/DashboardViewModelTests.cs` | Metric loading and empty/error state tests. | Open. |
| Item management | Item service, repository, item ViewModel, edit/details workflows, and command tests are present. | Checkout row refresh inferred local quantity/status after database toggle. | UI could show stale quantity, holder, checkout count, or status after a successful checkout/check-in. | Refresh the row from `GetItemByIDAsync` after a successful toggle and copy persisted state into the displayed row. | `ViewModels/ItemManagementViewModel.cs`, `Data/ItemRepository.cs`, `InventoryManagementApp.Tests/ItemManagementViewModelCheckoutRefreshTests.cs` | Regression test proving checkout uses persisted item state. | Fixed this run. |
| Item search | Dense table search page exists with results, checked-out panel, search intelligence, keyboard shortcuts, and print actions. | Runtime screenshot review blocked; recent search intelligence is session-only. | Search can look correct statically but still fail bindings, focus, or print preview at runtime. | Run WPF screenshot pass; continue tightening table density and keyboard behavior. | `Views/Pages/ItemSearchPage.xaml`, `Views/Pages/ItemSearchPage.xaml.cs`, `ViewModels/ItemManagementViewModel.cs` | Search binding, selection, context menu, and print empty-state tests. | Recently improved; needs runtime UI review. |
| Item edit | Edit dialog path clones selected item before saving. | Edit failure handling catches auth but not all validation/database exceptions in the main edit path. | Save failures may feel abrupt or leave users unsure whether edits persisted. | Add user-safe error dialog for failed edit saves and keep selected row stable. | `ViewModels/ItemManagementViewModel.cs`, `Services/DialogService.cs`, edit item dialog XAML | Tests for duplicate item number, invalid quantity, and cancelled edit. | Recommended next. |
| Customers | Customer service uses SQLite and import transaction; ViewModel/page/tests exist. | Search currently appears contact-light in service SQL and static UI review only. | Users may miss customers by contact name; delete/update failures may not be explained consistently. | Verify customer search fields and add error-safe dialog coverage. | `Services/Customers/CustomerService.cs`, `ViewModels/CustomerManagementViewModel.cs`, `Views/Pages/CustomersPage.xaml` | Customer search, add/edit/delete, import duplicate tests. | Open. |
| Rentals | Rental management ViewModel/page/tests exist; rental services and dialogs are wired. | Full flow review not possible here. | Duplicate or invalid active rentals are high business risk if service rules regress. | Prioritize rental service transaction and duplicate-active-rental tests. | `ViewModels/ManageRentalsViewModel.cs`, `Views/Pages/ManageRentalsPage.xaml`, rental services, `InventoryManagementApp.Tests/ManageRentalsViewModelTests.cs` | Active rental conflict, return, overdue, and history tests. | Open. |
| Checkout | Repository prevents rental-item checkout, zero-quantity checkout, and unauthorized check-in. ViewModel now refreshes persisted row state. | Need runtime validation and repository-level concurrency review. | Race conditions could still double-checkout under concurrent use. | Add repository tests around concurrent or stale checkout attempts and guarded update conditions. | `Data/ItemRepository.cs`, `Services/Items/ItemService.cs`, `ViewModels/ItemManagementViewModel.cs` | Repository/service tests for zero quantity, rental item, unauthorized holder, persisted refresh. | Improved this run. |
| Check-in | Repository blocks non-admin check-in by a different user and restores quantity. | ViewModel previously inferred state; now refreshes persisted state. | Stale holder/timestamp display after check-in. | Extend checkout-refresh test with check-in persisted state. | `Data/ItemRepository.cs`, `ViewModels/ItemManagementViewModel.cs` | Check-in refresh and unauthorized check-in tests. | Partially improved. |
| Checked-out items | Item search page includes a checked-out grid and print action. | Depends on `Items` collection refresh; not separately loaded from `GetCheckedOutItemsAsync` in the inspected ViewModel path. | Checked-out panel may omit items outside the current page/search set. | Consider loading checked-out items from service for the side panel. | `ViewModels/ItemManagementViewModel.cs`, `Views/Pages/ItemSearchPage.xaml` | Test that checked-out panel includes checked-out rows independent of search page data. | Open. |
| Import/export | Item/customer import/export services exist; customer CSV import uses a transaction. | Item CSV import inserts row-by-row without a transaction in the inspected path. | Partial item imports can corrupt a batch if a later row fails. | Wrap item CSV import in a transaction and return clear row errors. | `Services/Items/ItemService.cs`, `Services/Customers/CustomerService.cs`, `ViewModels/ImportExportViewModel.cs` | Bad row, duplicate, partial-failure rollback, export empty-data tests. | High-value follow-up. |
| Reports | Report ViewModel/page are present by search evidence. | Not inspected deeply this run. | Report output may fail on empty data or inconsistent date ranges. | Audit report generation and print/export paths. | `ViewModels/ReportsViewModel.cs`, reports page/service files | Empty report, normal report, invalid range tests. | Open. |
| Settings | Settings service and display settings tests exist. | UI and persistence review not completed. | Settings may fail silently or produce inconsistent labels/theme. | Verify settings persistence and failure messaging. | `ISettingsService`, settings service/page, `InventoryManagementApp.Tests/ItemDisplaySettingsTests.cs` | Theme, label, password iteration, and item card size tests. | Open. |
| Users/passwords | User management ViewModel/tests and password settings are present. | Password policy and configuration failure handling need focused audit. | Weak user feedback or unsafe password changes can lock users out. | Review password hashing/iteration settings and user-safe errors. | `ViewModels/UserManagementViewModel.cs`, user services, `InventoryManagementApp.Tests/UserManagementViewModelTests.cs` | Password validation, role, lockout/autologout tests. | Open. |
| Print preview | Print preview window and item search printing are present. | WPF launch and print preview screenshot not possible here. | Empty/normal print documents could render poorly or fail at runtime. | Run print preview with empty and normal data; keep table-style output. | `Views/Windows/PrintPreviewWindow.xaml`, `Views/Windows/PrintPreviewWindow.xaml.cs`, `Views/Pages/ItemSearchPage.xaml.cs`, `Services/DialogService.cs` | Print empty-state and document-construction tests. | Open. |
| Dialogs | Dialog service, info dialogs, edit/rent/history dialogs are present. | Some command paths log without showing a user-safe message, for example rental history failure. | Users can click a command and see nothing after a failure. | Standardize user-safe error dialogs for failed command paths. | `Services/DialogService.cs`, `Interfaces/IDialogService.cs`, ViewModels using dialogs | Dialog invocation tests for failure paths. | Open. |
| Startup | App startup tests exist by search; app startup code present. | Direct startup validation blocked. | Missing resource dictionaries or startup DB exceptions may only appear at launch. | Run WPF startup smoke test on Windows with clean DB. | `App.xaml`, `App.xaml.cs`, `DatabaseService.cs`, `InventoryManagementApp.Tests/AppStartupTests.cs` | Startup, resource dictionary, and clean database initialization tests. | Needs runtime validation. |
| Error handling | Services generally log and rethrow; ViewModels show user-safe messages in many command paths. | Some silent cancellations are intentional, but several broad failure paths only log. | Failures can look like ignored button clicks. | Audit command catch blocks and ensure actionable dialog messages for non-cancel failures. | ViewModels, `Services/DialogService.cs`, `DatabaseService.cs` | Error path tests for edit, rental history, import/export, email/print failures. | Open. |

## Implemented This Run

- Changed `ItemManagementViewModel.ToggleCheckOutAsync` so successful checkout/check-in refreshes the item from storage and copies the persisted state back into the row.
- Added `ItemManagementViewModelCheckoutRefreshTests.ToggleCheckOutCommand_UsesPersistedItemStateAfterSuccessfulToggle` to prove the UI row uses the service-returned persisted values instead of locally inferred quantity/status.

## Validation Attempted

- `dotnet --info`: failed because `dotnet` is not installed in the scheduled container.
- `dotnet restore`, `dotnet build`, and `dotnet test`: not run because the SDK is unavailable.
- WPF launch/screenshots: not run because this Linux container lacks WPF runtime support and no local checkout could be cloned due network proxy blocking GitHub clone/raw access.
- `scripts/check-banned-words.sh`: not run because the repository could not be cloned locally; run it on the next Windows or full-repo validation pass.

## Next Target

Item CSV import should be hardened next. The inspected item import path inserts rows one at a time without an explicit transaction, so a later failure can leave a partially imported batch. Wrap the batch in a transaction and add tests for bad rows, duplicates, and rollback behavior.
