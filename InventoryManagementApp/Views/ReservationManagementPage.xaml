<Page x:Class="InventoryManagementApp.Views.ReservationManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:InventoryManagementApp.ViewModels"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:ReservationManagementViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Reservation Management">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add" Command="{Binding AddReservationCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,5,0"/>
            <Button Content="Edit" Command="{Binding EditReservationCommand}" Margin="0,0,5,0"/>
            <Button Content="Cancel" Command="{Binding CancelReservationCommand}" Margin="0,0,5,0"/>
            <Button Content="Fulfill" Command="{Binding FulfillReservationCommand}" Margin="0,0,5,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,5,0}"/>

            <ComboBox ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Width="150" Margin="10,0,5,0"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding FilteredReservations}" 
                  SelectedItem="{Binding SelectedReservation}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Number" Binding="{Binding ItemNumber}" Width="*"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat='g'}" Width="*"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat='g'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>