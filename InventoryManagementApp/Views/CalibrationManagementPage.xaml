<Page x:Class="InventoryManagementApp.Views.CalibrationManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:InventoryManagementApp.ViewModels"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:CalibrationManagementViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Calibration Management">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add" Command="{Binding AddCalibrationCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,5,0"/>
            <Button Content="Edit" Command="{Binding EditCalibrationCommand}" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteCalibrationCommand}" Margin="0,0,5,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,5,0}"/>

            <ComboBox ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Width="150" Margin="10,0,5,0"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding FilteredCalibrationRecords}" 
                  SelectedItem="{Binding SelectedRecord}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Number" Binding="{Binding ItemNumber}" Width="*"/>
                <DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Calibration Date" Binding="{Binding CalibrationDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="Next Due Date" Binding="{Binding NextCalibrationDue, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="Performed By" Binding="{Binding PerformedBy}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page> 