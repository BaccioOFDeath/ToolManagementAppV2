<Window x:Class="InventoryManagementApp.Views.Windows.CalibrationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:InventoryManagementApp.Views.Controls"
        Title="Calibration"
        Width="640"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Item #" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CalibrationRecord.ItemNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Item Name" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CalibrationRecord.ItemName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Calibration Date" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding CalibrationRecord.CalibrationDate}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Next Due" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding CalibrationRecord.NextCalibrationDue}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Calibrated By" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CalibrationRecord.CalibratedBy, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Certificate #" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CalibrationRecord.CertificateNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Result" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,8"/>
            <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding ResultOptions}" SelectedItem="{Binding CalibrationRecord.Result}" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Notes" Style="{StaticResource LabelTextBlock}" Margin="0,0,8,0"/>
            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding CalibrationRecord.Notes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80"/>
        </Grid>

        <controls:SaveCancelBar Grid.Row="1"/>
    </Grid>
</Window>
