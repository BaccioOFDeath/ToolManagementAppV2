<Window x:Class="InventoryManagementApp.Views.Windows.MaintenanceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:InventoryManagementApp.Views.Controls"
        Title="Maintenance"
        Width="680"
        Height="430"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCard}" Margin="0,0,0,8">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Maintenance record" Style="{StaticResource PageTitleTextBlock}"/>
                    <TextBlock Text="Schedule work, capture completion details, and keep service notes with the item." Style="{StaticResource CaptionTextBlock}"/>
                </StackPanel>
                <TextBlock DockPanel.Dock="Right"
                           Text="{Binding MaintenanceRecord.Status}"
                           Style="{StaticResource CaptionTextBlock}"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="118"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="118"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Item #" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MaintenanceRecord.ItemNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Item Name" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding MaintenanceRecord.ItemName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Type" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding MaintenanceTypeOptions}" SelectedItem="{Binding MaintenanceRecord.MaintenanceType}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="1" Grid.Column="3" Text="Status" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <ComboBox Grid.Row="1" Grid.Column="4" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding MaintenanceRecord.Status}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Scheduled" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding MaintenanceRecord.ScheduledDate}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="2" Grid.Column="3" Text="Completed" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <DatePicker Grid.Row="2" Grid.Column="4" SelectedDate="{Binding MaintenanceRecord.CompletedDate}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Performed By" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding MaintenanceRecord.PerformedBy, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="3" Grid.Column="3" Text="Cost" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,8,8"/>
                <TextBox Grid.Row="3" Grid.Column="4" Text="{Binding MaintenanceRecord.Cost, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="4" Grid.ColumnSpan="5" Text="Notes" Style="{StaticResource SectionHeader}" Margin="0,4,0,4"/>
                <TextBox Grid.Row="5"
                         Grid.ColumnSpan="5"
                         Text="{Binding MaintenanceRecord.Notes, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <controls:SaveCancelBar Grid.Row="2"/>
    </Grid>
</Window>
