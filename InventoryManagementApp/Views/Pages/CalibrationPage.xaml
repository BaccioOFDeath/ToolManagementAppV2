<Page x:Class="InventoryManagementApp.Views.Pages.CalibrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource MainContentBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="CalibrationMetricCard" TargetType="Border" BasedOn="{StaticResource PageHeaderStatCard}"/>
    </Page.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource PageHeaderBand}" Background="{DynamicResource PageHeaderCalibrationBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.15*" MinWidth="0"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="2.85*" MinWidth="0"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Text="Calibration" Style="{StaticResource PageTitleTextBlock}" TextWrapping="Wrap"/>
                    <TextBlock Text="Track certificate readiness, overdue renewals, shelf-release status, and technician handoffs from one calibration register."
                               Style="{StaticResource CaptionTextBlock}"
                               TextWrapping="Wrap"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <WrapPanel Grid.Column="2" Style="{StaticResource PageHeaderStatsPanel}">
                    <Border Style="{StaticResource CalibrationMetricCard}">
                        <StackPanel>
                            <TextBlock Text="REGISTER" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding CalibrationResultsSummary}" FontSize="15" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CalibrationMetricCard}">
                        <StackPanel>
                            <TextBlock Text="DUE STATE" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding CalibrationBacklogSummary}" FontSize="15" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CalibrationMetricCard}">
                        <StackPanel>
                            <TextBlock Text="SELECTED" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding SelectedRecord.StatusDisplay, TargetNullValue='No certificate selected', FallbackValue='No certificate selected'}" FontSize="15" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CalibrationMetricCard}">
                        <StackPanel>
                            <TextBlock Text="PRINT" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding CalibrationPrintStatus}" FontSize="15" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ToolbarCard}" Padding="10,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DockPanel LastChildFill="True">
                    <TextBlock Text="Certificate actions" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <Button Style="{StaticResource PrimaryButton}" Content="Add Record" Command="{Binding AddCalibrationCommand}" Margin="0,0,4,4"/>
                    </WrapPanel>
                </DockPanel>
                <DockPanel Grid.Row="1" LastChildFill="True" Margin="0,4,0,0">
                    <TextBlock Text="Find and filter" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <TextBox Width="250" MinWidth="190" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,4" ToolTip="Search item number, name, certificate, calibrated by, standard, result, or notes"/>
                        <ComboBox Width="175" MinWidth="145" ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Margin="0,0,6,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Overdue" Command="{Binding ShowOverdueCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Due Soon" Command="{Binding ShowDueSoonCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Current" Command="{Binding ShowCurrentCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Clear" Command="{Binding ClearSearchCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Print Due" Command="{Binding PrintCalibrationListCommand}" Margin="0,0,4,4"/>
                    </WrapPanel>
                </DockPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.55*" MinWidth="0"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="01 Calibration Register" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Prioritizes overdue and due-soon certificates before current shelf-ready items" Style="{StaticResource CaptionTextBlock}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding CalibrationResultsSummary}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource DesktopPaneSubheader}">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding CalibrationBacklogSummary}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding CalibrationPrintStatus}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>
                    <DataGrid x:Name="CalibrationGrid"
                              Grid.Row="2"
                              ItemsSource="{Binding FilteredCalibrationRecords}"
                              SelectedItem="{Binding SelectedRecord}"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              Style="{StaticResource VirtualizedDataGridStyle}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <EventSetter Event="MouseDoubleClick" Handler="CalibrationRow_MouseDoubleClick"/>
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="CalibrationRow_PreviewMouseRightButtonDown"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Certificate item" Width="2.1*" MinWidth="190">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding ItemName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding ItemNumber}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Due window" Width="155" MinWidth="145">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding NextCalibrationDue, StringFormat={}{0:yyyy-MM-dd}}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding StatusDisplay}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Certificate" Width="2*" MinWidth="185">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding CertificateNumber}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Standard}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Last calibration" Width="160" MinWidth="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding CalibrationDate, StringFormat={}{0:yyyy-MM-dd}}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding CalibratedBy, TargetNullValue='Calibrator not set', FallbackValue='Calibrator not set'}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="90" MinWidth="84"/>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu Style="{StaticResource {x:Type ContextMenu}}"
                                        DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Open Details" Command="{Binding OpenCalibrationDetailsCommand}"/>
                                <MenuItem Header="Copy Calibration Handoff" Command="{Binding CopySelectedCalibrationCommand}"/>
                                <MenuItem Header="Print Record" Command="{Binding PrintSelectedCalibrationCommand}"/>
                                <Separator/>
                                <MenuItem Header="Edit" Command="{Binding EditCalibrationCommand}"/>
                                <MenuItem Header="Print Due Report" Command="{Binding PrintCalibrationListCommand}"/>
                                <MenuItem Header="Delete" Command="{Binding DeleteCalibrationCommand}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                    <Border Grid.Row="2" MaxWidth="360" MinHeight="130" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="16">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding FilteredCalibrationRecords.Count}" Value="0"/>
                                            <Condition Binding="{Binding IsLoading}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="{Binding CalibrationEmptyTitle}" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding CalibrationEmptyMessage}"
                                       Style="{StaticResource CaptionTextBlock}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" MaxWidth="380" MinHeight="118" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="16">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="Loading calibration register" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Certificate actions and due-report printing are paused until the latest saved rows are ready."
                                       Style="{StaticResource CaptionTextBlock}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="6"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource BorderBrushBase}"/>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="02 Certificate Handoff" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Selected certificate, release timing, and shelf checklist" Style="{StaticResource CaptionTextBlock}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Text="Issue-ready check" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="14">
                            <Border Style="{StaticResource DesktopSummaryCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="SELECTED CERTIFICATE" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBlock Text="{Binding SelectedRecord.ItemName, TargetNullValue='No calibration selected', FallbackValue='No calibration selected'}"
                                               FontSize="18"
                                               FontWeight="SemiBold"
                                               TextWrapping="Wrap"
                                               Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding SelectedRecordSummary}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="Certificate" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding SelectedCalibrationDetail}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="Timing and owner" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding SelectedCalibrationTimingSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="Next action" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding SelectedCalibrationNextAction}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <WrapPanel Margin="0,0,0,8">
                                <Button Style="{StaticResource PrimaryButton}" Content="Details" Command="{Binding OpenCalibrationDetailsCommand}" Margin="0,0,4,4"/>
                                <Button Style="{StaticResource PrimaryButton}" Content="Edit" Command="{Binding EditCalibrationCommand}" Margin="0,0,4,4"/>
                                <Button Style="{StaticResource GhostButton}" Content="Copy" Command="{Binding CopySelectedCalibrationCommand}" Margin="0,0,4,4"/>
                                <Button Style="{StaticResource GhostButton}" Content="Print" Command="{Binding PrintSelectedCalibrationCommand}" Margin="0,0,4,4"/>
                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>
                    <Border Grid.Row="2" Style="{StaticResource DesktopPaneSubheader}" BorderThickness="0,1,0,0">
                        <WrapPanel>
                            <Button Style="{StaticResource PrimaryButton}" Content="Print Due" Command="{Binding PrintCalibrationListCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource GhostButton}" Content="Clear Search" Command="{Binding ClearSearchCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource GhostButton}" Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,4,4"/>
                        </WrapPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Page>
