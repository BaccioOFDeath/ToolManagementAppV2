<!-- Views/ReportsPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Style="{StaticResource Card}">
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="260" ItemsSource="{Binding ReportTypes}" SelectedItem="{Binding SelectedReport}" ItemContainerStyle="{StaticResource DropdownItemStyle}"/>
                <Button Style="{StaticResource PrimaryButton}" Content="Run" Command="{Binding RunReportCommand}" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding ReportResults}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      Style="{StaticResource VirtualizedDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource {x:Type DataGridColumnHeader}}"/>
        </Border>
    </Grid>
</Page>
