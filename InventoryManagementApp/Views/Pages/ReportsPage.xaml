<!-- Views/ReportsPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource MainContentBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="ReportsStatCard" TargetType="Border" BasedOn="{StaticResource DesktopSummaryCard}">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="MinHeight" Value="78"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="MaxWidth" Value="240"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
        <Style x:Key="ReportsDetailCard" TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="ReportsDetailText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCard}" Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.15*" MinWidth="0"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" MinWidth="0">
                    <TextBlock Text="Reports Workbench" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="Run operational reports, inspect the highest-priority rows, then jump to the exact source page for follow-up."
                               Style="{StaticResource CaptionTextBlock}"
                               TextWrapping="Wrap"
                               Margin="0,3,0,0"/>
                </StackPanel>
                <WrapPanel Grid.Column="2" HorizontalAlignment="Right">
                    <Border Style="{StaticResource ReportsStatCard}">
                        <StackPanel>
                            <TextBlock Text="REPORT" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding SelectedReport, TargetNullValue=Select report, FallbackValue=Select report}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ReportsStatCard}">
                        <StackPanel>
                            <TextBlock Text="ROWS" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding ReportLineCount, StringFormat={}{0} action row(s)}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ReportsStatCard}">
                        <StackPanel>
                            <TextBlock Text="DESTINATION" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding SelectedLineDestination}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ReportsStatCard}">
                        <StackPanel>
                            <TextBlock Text="LAST RUN" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding LastRunText}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ToolbarCard}" Padding="10,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DockPanel LastChildFill="True">
                    <TextBlock Text="Report selector" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <ComboBox Width="250"
                                  MinWidth="200"
                                  ItemsSource="{Binding ReportTypes}"
                                  SelectedItem="{Binding SelectedReport}"
                                  ItemContainerStyle="{StaticResource DropdownItemStyle}"
                                  ToolTip="Choose the report to run"
                                  Margin="0,0,6,4"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="Run Report" Command="{Binding RunReportCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Open Source Page" Click="OpenSourcePage_Click" IsEnabled="{Binding CanUseReportRows}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Clear" Command="{Binding ClearReportCommand}" Margin="0,0,4,4"/>
                    </WrapPanel>
                </DockPanel>
                <DockPanel Grid.Row="1" LastChildFill="True" Margin="0,4,0,0">
                    <TextBlock Text="Output actions" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <Button Style="{StaticResource PrimaryButton}" Content="Print Report" Click="PrintReport_Click" IsEnabled="{Binding CanPrintCurrentReport}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Copy Handoff" Click="CopySelectedRow_Click" IsEnabled="{Binding CanUseReportRows}" Margin="0,0,4,4"/>
                        <TextBlock Text="{Binding ReportStatus}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="6,1,0,4"/>
                    </WrapPanel>
                </DockPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.55*" MinWidth="0"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left" MinWidth="0">
                                <TextBlock Text="01 Report Results" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Review generated report lines, priority wording, destination, and next action before opening the source workflow." Style="{StaticResource CaptionTextBlock}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock Text="{Binding ReportLineCount, StringFormat={}{0} row(s)}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource DesktopPaneSubheader}">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding ReportTitle}" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding ReportSubtitle}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <DataGrid x:Name="ReportGrid"
                              Grid.Row="2"
                              ItemsSource="{Binding ReportLines}"
                              SelectedItem="{Binding SelectedReportLine, Mode=TwoWay}"
                              IsReadOnly="True"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              MouseDoubleClick="ReportGrid_MouseDoubleClick"
                              PreviewMouseRightButtonDown="ReportGrid_PreviewMouseRightButtonDown"
                              Style="{StaticResource VirtualizedDataGridStyle}">
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Open Source Page" Click="OpenSourcePage_Click" IsEnabled="{Binding CanUseReportRows}"/>
                                <MenuItem Header="Copy Handoff" Click="CopySelectedRow_Click" IsEnabled="{Binding CanUseReportRows}"/>
                                <MenuItem Header="Print Report" Click="PrintReport_Click" IsEnabled="{Binding CanPrintCurrentReport}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="48" MinWidth="44"/>
                            <DataGridTemplateColumn Header="Type / Destination" Width="1.05*" MinWidth="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding Category}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding DestinationName, StringFormat=Open {0}}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Report Detail" Width="2.15*" MinWidth="240">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding Text}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding ActionHint, StringFormat=Next: {0}}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Destination" Binding="{Binding DestinationName}" Width="120" MinWidth="110"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" MaxWidth="360" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding ReportLineCount}" Value="0"/>
                                            <Condition Binding="{Binding IsBusy}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="No report rows are ready" Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
                            <TextBlock Text="Choose a report, run it, then use this list to open source pages, copy handoffs, or print a report packet." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" MaxWidth="380" MinHeight="118" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="16">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="Generating report" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Report row routing, copying, and print preview are paused until the latest generated rows are ready."
                                       Style="{StaticResource CaptionTextBlock}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="3" Style="{StaticResource DesktopPaneSubheader}" BorderThickness="0,1,0,0">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding ReportSummary}" Style="{StaticResource CaptionTextBlock}" DockPanel.Dock="Left" TextWrapping="Wrap" Margin="0,0,10,0"/>
                            <Button Style="{StaticResource GhostButton}" Content="Clear" Command="{Binding ClearReportCommand}" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource BorderBrushBase}"/>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource DesktopPaneHeader}">
                        <StackPanel>
                            <TextBlock Text="02 Row Handoff" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <TextBlock Text="Keep the report context, source page, and next action visible while triaging generated rows." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="8">
                            <Border Style="{StaticResource ReportsDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Selected Row" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBlock Text="{Binding SelectedLineDestination, StringFormat=Destination: {0}}" Style="{StaticResource PageTitleTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding SelectedLineDetail}" Style="{StaticResource ReportsDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ReportsDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Report Summary" Style="{StaticResource SectionHeader}"/>
                                    <TextBlock Text="{Binding ReportSummary}" Style="{StaticResource ReportsDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ReportsDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Operator Path" Style="{StaticResource SectionHeader}"/>
                                    <TextBlock Text="{Binding ReportOperatorPath}" Style="{StaticResource ReportsDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ReportsDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Handoff Text" Style="{StaticResource SectionHeader}"/>
                                    <TextBox Text="{Binding SelectedLineHandoff, Mode=OneWay}"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="True"
                                             MinHeight="130"
                                             MaxHeight="260"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Disabled"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopInsetCard}" Padding="10" Margin="0,0,0,8">
                                <WrapPanel>
                                    <Button Style="{StaticResource PrimaryButton}" Content="Open Source" Click="OpenSourcePage_Click" IsEnabled="{Binding CanUseReportRows}" Margin="0,0,4,4"/>
                                    <Button Style="{StaticResource GhostButton}" Content="Copy Handoff" Click="CopySelectedRow_Click" IsEnabled="{Binding CanUseReportRows}" Margin="0,0,4,4"/>
                                    <Button Style="{StaticResource GhostButton}" Content="Print Report" Click="PrintReport_Click" IsEnabled="{Binding CanPrintCurrentReport}" Margin="0,0,4,4"/>
                                </WrapPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3"
                Style="{StaticResource ToolbarCard}"
                Margin="0,6,0,0"
                Padding="8,5">
            <DockPanel LastChildFill="True">
                <ProgressBar Width="140"
                             Height="14"
                             DockPanel.Dock="Right"
                             IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding ReportStatus}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </DockPanel>
        </Border>
    </Grid>
</Page>