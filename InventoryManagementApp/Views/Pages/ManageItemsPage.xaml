<!-- Views/ManageItemsPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.ManageItemsPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:helpers="clr-namespace:InventoryManagementApp.Utilities.Helpers"
      xmlns:pages="clr-namespace:InventoryManagementApp.Views.Pages"
      Background="{DynamicResource MainContentBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="DirectoryStatCard" TargetType="Border" BasedOn="{StaticResource DesktopSummaryCard}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinHeight" Value="54"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="MaxWidth" Value="230"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
        </Style>
        <Style x:Key="DirectoryStatValueText" TargetType="TextBlock" BasedOn="{StaticResource StatisticValueTextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="DirectoryDetailText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        <Style x:Key="LoadingAwarePrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectoryBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LoadingAwareGhostButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectoryBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DirectoryStatusValueText" TargetType="TextBlock" BasedOn="{StaticResource DirectoryStatValueText}">
            <Setter Property="Text" Value="Ready"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectoryBusy}" Value="True">
                    <Setter Property="Text" Value="Loading rows"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DirectoryStatusDetailText" TargetType="TextBlock" BasedOn="{StaticResource DirectoryDetailText}">
            <Setter Property="Text" Value="Actions use the currently loaded page of rows"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectoryBusy}" Value="True">
                    <Setter Property="Text" Value="Row actions pause until the directory finishes loading"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AvailabilityTextBlock" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding AvailabilityStatus}" Value="Checked Out">
                    <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding AvailabilityStatus}" Value="Rented">
                    <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding AvailabilityStatus}" Value="Unavailable">
                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding AvailabilityStatus}" Value="Incomplete">
                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DirectoryGridTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </Style>
    </Page.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCard}" Padding="8,5" Margin="0,0,0,5">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center" MinWidth="220" MaxWidth="460">
                    <TextBlock Text="Manage Items" Style="{StaticResource PageTitleTextBlock}" TextWrapping="Wrap"/>
                    <TextBlock Text="Maintain the item directory, review current availability, and save inline stock/location edits before staff use live records." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                </StackPanel>
                <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="12,0,0,0">
                    <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="New" Command="{Binding NewItemCommand}" Margin="0,0,4,4"/>
                    <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="Mobile Capture" Command="{Binding OpenMobileCaptureCommand}" Margin="0,0,4,4"/>
                    <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="Edit" Command="{Binding EditItemCommand}" Margin="0,0,4,4"/>
                    <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Details" Command="{Binding ViewDetailsCommand}" Margin="0,0,4,4"/>
                    <Button Style="{StaticResource LoadingAwareGhostButton}" Content="History" Command="{Binding OpenRentalHistoryCommand}" Margin="0,0,4,4"/>
                    <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Delete" Command="{Binding DeleteSelectedItemCommand}" CommandParameter="{Binding SelectedItem}" Margin="0,0,4,4"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <WrapPanel Grid.Row="1" Margin="0,0,0,5">
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Loaded Rows" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding Items.Count}" Style="{StaticResource DirectoryStatValueText}"/>
                    <TextBlock Text="Virtualized directory rows currently in memory" Style="{StaticResource DirectoryDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Pending Edits" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding PendingEdits.Count}" Style="{StaticResource DirectoryStatValueText}"/>
                    <TextBlock Text="Inline quantity, price, or location changes awaiting save" Style="{StaticResource DirectoryDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Missing Images" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding MissingImageCount}" Style="{StaticResource DirectoryStatValueText}"/>
                    <TextBlock Text="Loaded rows still using the default item photo" Style="{StaticResource DirectoryDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Page Size" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding PageSize}" Style="{StaticResource DirectoryStatValueText}"/>
                    <TextBlock Text="Rows requested per directory load" Style="{StaticResource DirectoryDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Directory Status" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Style="{StaticResource DirectoryStatusValueText}"/>
                    <TextBlock Style="{StaticResource DirectoryStatusDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DirectoryStatCard}">
                <StackPanel>
                    <TextBlock Text="Selected Item" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding SelectedItem.Name, TargetNullValue=No item selected}" Style="{StaticResource DirectoryStatValueText}"/>
                    <TextBlock Text="{Binding SelectedItem.AvailabilityDetail, TargetNullValue='Select a row to review availability and next actions.'}" Style="{StaticResource DirectoryDetailText}"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.7*" MinWidth="0"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" MinWidth="190" MaxWidth="330">
                                <TextBlock Text="Inventory Directory" Style="{StaticResource SectionHeader}" Margin="0" TextWrapping="Wrap"/>
                                <TextBlock Text="Edit stock, price, and location inline." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Filter, StringFormat='Current filter: {0}', TargetNullValue='Current filter: All items'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource DesktopPaneSubheader}">
                        <DockPanel LastChildFill="True">
                            <WrapPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="Filter" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,6,4"/>
                                <pages:SearchBar Width="240"
                                                 MinWidth="180"
                                                 IsEnabled="{Binding IsDirectoryBusy, Converter={StaticResource InverseBooleanConverter}}"
                                                 Text="{Binding Filter}"
                                                 SearchCommand="{Binding SearchCommand}"
                                                 SearchLabel="{Binding ItemLabelPlural, Source={x:Static helpers:LabelProvider.Instance}, StringFormat=Search {0}}"/>
                                <TextBlock Text="Sort" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="12,0,6,4"/>
                                <ComboBox Width="110"
                                          MinWidth="96"
                                          IsEnabled="{Binding IsDirectoryBusy, Converter={StaticResource InverseBooleanConverter}}"
                                          ItemsSource="{Binding SortOptions}"
                                          SelectedItem="{Binding SelectedSortOption}"
                                          DisplayMemberPath="DisplayName"/>
                                <TextBlock Text="Rows" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="12,0,6,4"/>
                                <TextBox Width="56" MinWidth="48" IsEnabled="{Binding IsDirectoryBusy, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding PageSize, UpdateSourceTrigger=LostFocus}"/>
                            </WrapPanel>
                            <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="12,0,0,0">
                                <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="Save Changes" Command="{Binding CommitChangesCommand}" Margin="0,0,4,4"/>
                                <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Details" Command="{Binding ViewDetailsCommand}" Margin="0,0,4,4"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>
                    <DataGrid x:Name="ItemDirectoryGrid"
                              Grid.Row="2"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem}"
                              SelectionMode="Extended"
                              SelectionUnit="FullRow"
                              IsReadOnly="False"
                              AutoGenerateColumns="False"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.ScrollUnit="Item"
                              VirtualizingPanel.CacheLength="1"
                              VirtualizingPanel.CacheLengthUnit="Page"
                              RowDetailsVisibilityMode="Collapsed"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              CanUserResizeColumns="True"
                              CanUserReorderColumns="True"
                              CanUserSortColumns="True"
                              ClipboardCopyMode="IncludeHeader"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.ScrollChanged="ItemDirectoryGrid_ScrollChanged"
                              Style="{StaticResource VirtualizedDataGridStyle}">
                        <DataGrid.RowHeight>56</DataGrid.RowHeight>
                        <DataGrid.Resources>
                            <helpers:BindingProxy x:Key="VmProxy" Data="{Binding}"/>
                        </DataGrid.Resources>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <EventSetter Event="PreviewMouseRightButtonDown"
                                             Handler="DataGridRow_PreviewMouseRightButtonDown"/>
                                <EventSetter Event="MouseDoubleClick"
                                             Handler="DataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Photo" Visibility="{Binding Data.ShowImage, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Width="48" Height="48" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrushAlt}" BorderThickness="1" Margin="0,2">
                                            <Image Source="{Binding Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=item}" Stretch="UniformToFill" ClipToBounds="True"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Number" Visibility="{Binding Data.ShowItemNumber, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding ItemNumber, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="110"/>
                            <DataGridTextColumn Header="Part Number" Visibility="{Binding Data.ShowPartNumber, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding PartNumber, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="120"/>
                            <DataGridTextColumn Header="Name" Visibility="{Binding Data.ShowName, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding Name, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="210"/>
                            <DataGridTextColumn Header="Brand" Visibility="{Binding Data.ShowBrand, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding Brand, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="120"/>
                            <DataGridTemplateColumn Header="Status" Width="125">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource AvailabilityTextBlock}" Text="{Binding AvailabilityStatus}" TextTrimming="CharacterEllipsis" ToolTip="{Binding AvailabilityDetail}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Quantity" Visibility="{Binding Data.ShowQuantityOnHand, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding QuantityOnHand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="80"/>
                            <DataGridTextColumn Header="Location" Visibility="{Binding Data.ShowLocation, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding Location, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="130"/>
                            <DataGridTextColumn Header="Unit Price" Visibility="{Binding Data.ShowPrice, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="90"/>
                            <DataGridTextColumn Header="Activity" Binding="{Binding ActivitySummary, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="150"/>
                            <DataGridTextColumn Header="Notes" Visibility="{Binding Data.ShowNotes, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}" Binding="{Binding Notes, Mode=OneWay}" ElementStyle="{StaticResource DirectoryGridTextBlockStyle}" Width="*"/>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu Style="{StaticResource {x:Type ContextMenu}}"
                                        DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="View details"
                                          IsEnabled="{Binding PlacementTarget.DataContext.IsDirectoryBusy, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBooleanConverter}}"
                                          Command="{Binding PlacementTarget.DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="Edit item"
                                          IsEnabled="{Binding PlacementTarget.DataContext.IsDirectoryBusy, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBooleanConverter}}"
                                          Command="{Binding PlacementTarget.DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="Rental history"
                                          IsEnabled="{Binding PlacementTarget.DataContext.IsDirectoryBusy, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBooleanConverter}}"
                                          Command="{Binding PlacementTarget.DataContext.OpenRentalHistoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <Separator/>
                                <MenuItem Header="Save pending changes"
                                          IsEnabled="{Binding PlacementTarget.DataContext.IsDirectoryBusy, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBooleanConverter}}"
                                          Command="{Binding PlacementTarget.DataContext.CommitChangesCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="Delete selected"
                                          IsEnabled="{Binding PlacementTarget.DataContext.IsDirectoryBusy, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBooleanConverter}}"
                                          Command="{Binding PlacementTarget.DataContext.DeleteItemsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                    <Border Grid.Row="2" MaxWidth="320" MinHeight="120" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsDirectoryBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="No matching items" Style="{StaticResource SectionHeader}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Adjust the filter or create a new item to begin building the directory." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,8"/>
                            <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="New Item" Command="{Binding NewItemCommand}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" MaxWidth="360" MinHeight="120" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsDirectoryBusy, Converter={StaticResource BoolToVis}}" IsHitTestVisible="False">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                <Setter Property="Opacity" Value="0.96"/>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="Loading item rows" Style="{StaticResource SectionHeader}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="The directory is refreshing. Row actions will resume when the current page is ready." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,8"/>
                            <ProgressBar IsIndeterminate="True" Height="4" MinWidth="180"/>
                        </StackPanel>
                    </Border>
                    <ProgressBar Grid.Row="3"
                                 IsIndeterminate="True"
                                 Height="4"
                                 Margin="0,4,0,0"
                                 Visibility="{Binding IsDirectoryBusy, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource DesktopPaneHeader}">
                        <StackPanel>
                            <TextBlock Text="Selected Item Handoff" Style="{StaticResource SectionHeader}" Margin="0" TextWrapping="Wrap"/>
                            <TextBlock Text="Confirm before editing or opening history." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    <Border DockPanel.Dock="Bottom" Style="{StaticResource DesktopSectionActionStrip}">
                        <WrapPanel>
                            <Button Style="{StaticResource LoadingAwarePrimaryButton}" Content="Edit" Command="{Binding EditItemCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Details" Command="{Binding ViewDetailsCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource LoadingAwareGhostButton}" Content="History" Command="{Binding OpenRentalHistoryCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Save" Command="{Binding CommitChangesCommand}" Margin="0,0,4,4"/>
                            <Button Style="{StaticResource LoadingAwareGhostButton}" Content="Delete" Command="{Binding DeleteSelectedItemCommand}" CommandParameter="{Binding SelectedItem}" Margin="0,0,4,4"/>
                        </WrapPanel>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="10">
                            <Border Style="{StaticResource DesktopSummaryCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{Binding SelectedItem.Name, TargetNullValue=No item selected}" Style="{StaticResource SubheadingTextBlock}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding SelectedItem.SearchIdentityLine, TargetNullValue=Select a row in the directory.}" Style="{StaticResource DirectoryDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Availability" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBlock Text="{Binding SelectedItem.AvailabilityStatus, TargetNullValue=Not selected}" Style="{StaticResource DirectoryStatValueText}"/>
                                    <TextBlock Text="{Binding SelectedItem.AvailabilityDetail, TargetNullValue='Select an item to review stock, holder, and next-step context.'}" Style="{StaticResource DirectoryDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource DesktopNoteCard}">
                                    <StackPanel>
                                        <TextBlock Text="Stock" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding SelectedItem.StockSummary, TargetNullValue=No stock selected}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource DesktopNoteCard}">
                                    <StackPanel>
                                        <TextBlock Text="Holder" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding SelectedItem.HolderDisplay, TargetNullValue=No holder selected}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Location and keywords" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBlock Text="{Binding SelectedItem.SearchLocationLine, TargetNullValue=Select an item to review location and keyword context.}" Style="{StaticResource DirectoryDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopSummaryCard}">
                                <StackPanel>
                                    <TextBlock Text="Directory checklist" Style="{StaticResource SectionHeader}" TextWrapping="Wrap"/>
                                    <TextBlock Text="1. Filter or sort to the right record. 2. Confirm the selected item context. 3. Save pending inline edits before leaving the page." TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource ToolbarCard}" Padding="6,3" Margin="0,5,0,0">
            <WrapPanel>
                <TextBlock Text="{Binding Items.Count, StringFormat='Loaded rows: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding PageSize, StringFormat='Page size: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding PendingEdits.Count, StringFormat='Pending inline edits: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding MissingImageCount, StringFormat='Missing images in loaded rows: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding SelectedSortOption.DisplayName, StringFormat='Sort: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding Items.HasMoreItems, StringFormat='More rows available: {0}'}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center"/>
            </WrapPanel>
        </Border>
    </Grid>
</Page>
