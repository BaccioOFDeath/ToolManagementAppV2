<!-- Views/ActivityLogsPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.ActivityLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource MainContentBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="ActivityStatCard" TargetType="Border" BasedOn="{StaticResource DesktopSummaryCard}">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="MinHeight" Value="78"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="MaxWidth" Value="230"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
        <Style x:Key="ActivityDetailCard" TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="ActivityDetailText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCard}" Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" MinWidth="260"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="1.8*" MinWidth="320"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Activity Audit Workbench" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="Filter recent operational events, inspect the selected audit trail, and open the related workflow without losing row context."
                               Style="{StaticResource CaptionTextBlock}"
                               TextWrapping="Wrap"
                               Margin="0,3,0,0"/>
                </StackPanel>
                <WrapPanel Grid.Column="2" HorizontalAlignment="Right">
                    <Border Style="{StaticResource ActivityStatCard}">
                        <StackPanel>
                            <TextBlock Text="VISIBLE" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding FilteredLogCount, StringFormat={}{0} rows visible}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ActivityStatCard}">
                        <StackPanel>
                            <TextBlock Text="LOADED" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding TotalLogCount, StringFormat={}{0} recent rows}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ActivityStatCard}">
                        <StackPanel>
                            <TextBlock Text="SELECTED" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding SelectedLogActionGroup}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ActivityStatCard}">
                        <StackPanel>
                            <TextBlock Text="PRINT" Style="{StaticResource LabelTextBlock}"/>
                            <TextBlock Text="{Binding PrintStatusText}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ToolbarCard}" Padding="10,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DockPanel LastChildFill="True">
                    <TextBlock Text="Audit filters" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <TextBox x:Name="ActivitySearchBox"
                                 MinWidth="180"
                                 Width="250"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding CanChangeActivityFilters}"
                                 ToolTip="Filter by user, action, type, destination, user ID, or timestamp"
                                 Margin="0,0,6,4"/>
                        <ComboBox ItemsSource="{Binding UserFilters}"
                                  SelectedItem="{Binding SelectedUserFilter}"
                                  IsEnabled="{Binding CanChangeActivityFilters}"
                                  MinWidth="150"
                                  Width="170"
                                  ToolTip="Filter by user"
                                  Margin="0,0,6,4"/>
                        <ComboBox ItemsSource="{Binding ActionFilters}"
                                  SelectedItem="{Binding SelectedActionFilter}"
                                  IsEnabled="{Binding CanChangeActivityFilters}"
                                  MinWidth="165"
                                  Width="185"
                                  ToolTip="Filter by action group"
                                  Margin="0,0,6,4"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Clear" Command="{Binding ClearFiltersCommand}" Margin="0,0,4,4"/>
                    </WrapPanel>
                </DockPanel>
                <DockPanel Grid.Row="1" LastChildFill="True" Margin="0,4,0,0">
                    <TextBlock Text="Audit actions" Style="{StaticResource LabelTextBlock}" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <WrapPanel>
                        <Button Style="{StaticResource PrimaryButton}" Content="Open Related" Click="OpenRelatedPage_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Open Detail" Click="OpenSelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Copy Handoff" Click="CopySelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                        <Button Style="{StaticResource GhostButton}" Content="Print Results" Click="PrintLogs_Click" IsEnabled="{Binding CanPrintActivityRows}" Margin="0,0,4,4"/>
                        <TextBlock Text="{Binding ActivitySummary}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="6,1,0,4"/>
                    </WrapPanel>
                </DockPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.55*" MinWidth="0"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid MinWidth="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="01 Activity Directory" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Scan the current audit trail by user, event family, destination, and follow-up path." Style="{StaticResource CaptionTextBlock}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock Text="{Binding FilteredLogCount, StringFormat={}{0} visible}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource DesktopPaneSubheader}">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding ActivitySummary}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding LastLoadedText, StringFormat=Last refresh: {0}}" Style="{StaticResource CaptionTextBlock}" HorizontalAlignment="Right" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <DataGrid x:Name="ActivityGrid"
                              Grid.Row="2"
                              ItemsSource="{Binding FilteredLogs}"
                              SelectedItem="{Binding SelectedLog, Mode=TwoWay}"
                              IsReadOnly="True"
                              AutoGenerateColumns="False"
                              Style="{StaticResource VirtualizedDataGridStyle}"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              MouseDoubleClick="ActivityGrid_MouseDoubleClick">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="ActivityGridRow_PreviewMouseRightButtonDown"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Open Activity Detail" Click="OpenSelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}"/>
                                <MenuItem Header="Open Related Page" Click="OpenRelatedPage_Click" IsEnabled="{Binding CanUseSelectedLogActions}"/>
                                <MenuItem Header="Copy Handoff" Click="CopySelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}"/>
                                <Separator/>
                                <MenuItem Header="Print Current Results" Click="PrintLogs_Click" IsEnabled="{Binding CanPrintActivityRows}"/>
                                <MenuItem Header="Refresh" Click="RefreshLogs_Click" IsEnabled="{Binding CanRefreshActivityRows}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="When / User" Width="1.2*" MinWidth="210">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm}}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding UserName, TargetNullValue=Unknown user, FallbackValue=Unknown user}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="User ID" Binding="{Binding UserID}" Width="72" MinWidth="62"/>
                            <DataGridTemplateColumn Header="Audit Event" Width="2.3*" MinWidth="330">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,3">
                                            <TextBlock Text="{Binding Action}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="Double-click for detail or open the related workflow from the action bar." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" MaxWidth="360" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanShowActivityEmptyState}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="{Binding ActivityEmptyStateTitle}" Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding ActivityEmptyStateMessage}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" MaxWidth="380" MinHeight="118" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <ProgressBar IsIndeterminate="True" Height="6" Margin="0,0,0,10"/>
                            <TextBlock Text="Updating activity directory" Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding ActivityBusyMessage}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="3" Style="{StaticResource DesktopPaneSubheader}" BorderThickness="0,1,0,0">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding PrintStatusText}" Style="{StaticResource CaptionTextBlock}" DockPanel.Dock="Left" TextWrapping="Wrap" Margin="0,0,10,0"/>
                            <Button Style="{StaticResource GhostButton}" Content="Clear Filters" Command="{Binding ClearFiltersCommand}" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource BorderBrushBase}"/>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid MinWidth="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource DesktopPaneHeader}">
                        <StackPanel>
                            <TextBlock Text="02 Audit Handoff" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <TextBlock Text="Keep selected-row detail, destination, and follow-up instructions visible during audit review." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="8">
                            <Border Style="{StaticResource ActivityDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Selected Activity" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBlock Text="{Binding SelectedLogTitle}" Style="{StaticResource PageTitleTextBlock}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding SelectedLogTimestamp}" Style="{StaticResource ActivityDetailText}"/>
                                    <TextBlock Text="{Binding SelectedLogActionGroup}" Style="{StaticResource ActivityDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ActivityDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Next Action" Style="{StaticResource SectionHeader}"/>
                                    <TextBlock Text="{Binding SelectedLogNextAction}" Style="{StaticResource ActivityDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ActivityDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Related Workflow" Style="{StaticResource SectionHeader}"/>
                                    <TextBlock Text="{Binding SelectedLogDestinationName, StringFormat=Destination: {0}}" Style="{StaticResource ActivityDetailText}"/>
                                    <TextBlock Text="{Binding SelectedLogOperatorPath}" Style="{StaticResource ActivityDetailText}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource ActivityDetailCard}">
                                <StackPanel>
                                    <TextBlock Text="Handoff Text" Style="{StaticResource SectionHeader}"/>
                                    <TextBox Text="{Binding SelectedLogHandoff, Mode=OneWay}"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="True"
                                             MinHeight="130"
                                             MaxHeight="260"
                                             VerticalScrollBarVisibility="Auto"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopInsetCard}" Padding="10" Margin="0,0,0,8">
                                <WrapPanel>
                                    <Button Style="{StaticResource PrimaryButton}" Content="Open Related" Click="OpenRelatedPage_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                                    <Button Style="{StaticResource GhostButton}" Content="Open Detail" Click="OpenSelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                                    <Button Style="{StaticResource GhostButton}" Content="Copy Handoff" Click="CopySelectedLog_Click" IsEnabled="{Binding CanUseSelectedLogActions}" Margin="0,0,4,4"/>
                                    <Button Style="{StaticResource GhostButton}" Content="Print" Click="PrintLogs_Click" IsEnabled="{Binding CanPrintActivityRows}" Margin="0,0,4,4"/>
                                </WrapPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3"
                Style="{StaticResource ToolbarCard}"
                Margin="0,6,0,0"
                Padding="8,5">
            <DockPanel LastChildFill="True">
                <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding FilteredLogCount, StringFormat='Visible: {0}'}" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding TotalLogCount, StringFormat='Loaded: {0}'}" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding LastLoadedText, StringFormat='Last refresh: {0}'}" FontSize="11"/>
                </WrapPanel>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </DockPanel>
        </Border>
    </Grid>
</Page>
