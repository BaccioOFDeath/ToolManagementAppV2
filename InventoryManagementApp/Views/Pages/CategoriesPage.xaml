<!-- Views/Pages/CategoriesPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / editor -->
        <Border Style="{StaticResource ToolbarCard}">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBox Width="220"
                   Margin="0,0,8,0"
                   Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}"
                   ToolTip="Category name"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="New"    Command="{Binding AddCommand}"    Margin="0,0,8,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Save"   Command="{Binding SaveCommand}"   Margin="0,0,8,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Delete" Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource PrimaryButton}" Content="Refresh" Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Categories list -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                IsReadOnly="True"
                AutoGenerateColumns="False"
                Style="{StaticResource VirtualizedDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"   Binding="{Binding CategoryID}" Width="120"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"       Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Busy bar -->
        <ProgressBar Grid.Row="2"
                 IsIndeterminate="True"
                 Margin="0,6,0,0"
                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</Page>
