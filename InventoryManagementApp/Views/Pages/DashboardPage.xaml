<!-- Views/DashboardPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:helpers="clr-namespace:InventoryManagementApp.Utilities.Helpers"
      Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Overview" Style="{StaticResource SubheadingTextBlock}" Margin="0,0,0,8" VerticalAlignment="Center"/>
            <Button Grid.Column="1"
                    Style="{StaticResource PrimaryButton}"
                    Content="{Binding EditModeLabel}"
                    Command="{Binding ToggleEditModeCommand}"
                    Width="Auto"
                    Margin="12,0,0,8"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right"/>
        </Grid>
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="0" CornerRadius="16">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ItemsControl ItemsSource="{Binding StatCards}" ItemTemplate="{StaticResource StatCardTemplate}" ItemsPanel="{StaticResource WrapPanelItems}" Margin="0,8,0,0"/>
        </Border>
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="MinWidth" Value="220"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Cursor" Value="SizeAll"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#40FFC326" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Recent Activity" Style="{StaticResource SubheadingTextBlock}" Margin="0,0,0,8"/>
                    <ListView ItemsSource="{Binding RecentActivity}"
                              ItemTemplate="{StaticResource ActivityLogItemTemplate}"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              Style="{StaticResource CardListView}">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                    </ListView>
                </StackPanel>
            </Border>
            <GridSplitter Grid.Row="0"
                          Grid.Column="0"
                          Grid.ColumnSpan="1"
                          Style="{StaticResource EditGridSplitterStyle}"
                          Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Stretch"
                          Width="10"
                          Margin="-4,16,-4,16"
                          ShowsPreview="True"
                          Cursor="SizeWE"
                          Panel.ZIndex="1"/>
            <Border Grid.Column="1">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="MinWidth" Value="220"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Cursor" Value="SizeAll"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#40FFC326" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Quick Actions" Style="{StaticResource SubheadingTextBlock}" Margin="0,0,0,8"/>
                    <WrapPanel>
                        <Button Style="{StaticResource PrimaryButton}"
                                Width="Auto"
                                Content="{Binding ItemLabelSingular, Source={x:Static helpers:LabelProvider.Instance}}"
                                ContentStringFormat="New {0}"
                                Command="{Binding NewItemCommand}"
                                Margin="0,0,8,8"/>
                        <Button Style="{StaticResource PrimaryButton}"
                                Width="Auto"
                                Content="{Binding ItemLabelSingular, Source={x:Static helpers:LabelProvider.Instance}}"
                                ContentStringFormat="Rent {0}"
                                Command="{Binding OpenRentalsCommand}"
                                Margin="0,0,8,8"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Style="{StaticResource EditGridSplitterStyle}"
                          Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Stretch"
                          Width="10"
                          Margin="-4,16,-4,16"
                          ShowsPreview="True"
                          Cursor="SizeWE"
                          Panel.ZIndex="1"/>
            <Border Grid.Column="2">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="MinWidth" Value="220"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Cursor" Value="SizeAll"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#40FFC326" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Checked-Out Items" Style="{StaticResource SubheadingTextBlock}" Margin="0,0,0,8"/>
                    <ListView ItemsSource="{Binding CheckedOutItems}"
                              Style="{StaticResource CardListView}"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel.ToolTip>
                                        <Image Source="{Binding ImagePath, Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=item}"
                                               MaxWidth="150"
                                               MaxHeight="150"
                                               Stretch="Uniform"/>
                                    </StackPanel.ToolTip>
                                    <TextBlock Text="{Binding ItemNumber}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding CheckedOutBy}" Margin="0,0,8,0"/>
                                    <Button Content="Check In"
                                            Command="{Binding DataContext.CheckInItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                    </ListView>
                </StackPanel>
            </Border>
            <GridSplitter Grid.Row="0"
                          Grid.Column="2"
                          Style="{StaticResource EditGridSplitterStyle}"
                          Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Stretch"
                          Width="10"
                          Margin="-4,16,-4,16"
                          ShowsPreview="True"
                          Cursor="SizeWE"
                          Panel.ZIndex="1"/>
            <Border Grid.Column="3">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="MinWidth" Value="220"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Cursor" Value="SizeAll"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#40FFC326" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Rented Items" Style="{StaticResource SubheadingTextBlock}" Margin="0,0,0,8"/>
                    <ListView ItemsSource="{Binding RentedItems}"
                              Style="{StaticResource CardListView}"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel.ToolTip>
                                        <Image Source="{Binding ImagePath, Converter={StaticResource NullToDefaultImageConverter}, ConverterParameter=item}"
                                               MaxWidth="150"
                                               MaxHeight="150"
                                               Stretch="Uniform"/>
                                    </StackPanel.ToolTip>
                                    <TextBlock Text="{Binding ItemNumber}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding CustomerName}" Margin="0,0,8,0"/>
                                    <Button Content="Return"
                                            Command="{Binding DataContext.ReturnRentalCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
