<!-- Views/CustomersPage.xaml -->
<Page x:Class="InventoryManagementApp.Views.Pages.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:pages="clr-namespace:InventoryManagementApp.Views.Pages"
      Background="{DynamicResource MainContentBackgroundBrush}">
    <Page.Resources>
        <Style x:Key="CustomerStatCard" TargetType="Border" BasedOn="{StaticResource DesktopSummaryCard}">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="MinHeight" Value="74"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="MaxWidth" Value="250"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
        <Style x:Key="CustomerDetailText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        <Style x:Key="CustomerStatValueText" TargetType="TextBlock" BasedOn="{StaticResource HeadingTextBlock}">
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="MinHeight" Value="24"/>
        </Style>
    </Page.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCard}" Padding="14,12">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,18,0" MinWidth="220" MaxWidth="460">
                    <TextBlock Text="Customers" Style="{StaticResource PageTitleTextBlock}" TextWrapping="Wrap"/>
                    <TextBlock Text="Find customer records, verify contact details, and prepare advisor handoffs before rentals or service follow-up." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                </StackPanel>
                <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button Style="{StaticResource PrimaryButton}" Content="Add" Command="{Binding AddCustomerCommand}" IsEnabled="{Binding IsCustomerDirectoryActionAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Details" Command="{Binding OpenCustomerDetailsCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Edit" Command="{Binding EditCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource GhostButton}" Content="Copy Contact" Command="{Binding CopySelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource GhostButton}" Content="Print Sheet" Command="{Binding PrintSelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource GhostButton}" Content="Directory" Command="{Binding PrintCustomerDirectoryCommand}" IsEnabled="{Binding IsCustomerDirectoryPrintAvailable}" Margin="0,0,6,6"/>
                    <Button Style="{StaticResource GhostButton}" Content="Delete" Command="{Binding DeleteCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,0,6"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <WrapPanel Grid.Row="1" Margin="0,0,0,6">
            <Border Style="{StaticResource CustomerStatCard}">
                <StackPanel>
                    <TextBlock Text="Visible Customers" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding CustomerDirectoryVisibleCount}" Style="{StaticResource CustomerStatValueText}"/>
                    <TextBlock Text="{Binding CustomerVisibleWindowSummary}" Style="{StaticResource CustomerDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CustomerStatCard}">
                <StackPanel>
                    <TextBlock Text="Matched Customers" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding CustomerDirectoryMatchCount}" Style="{StaticResource CustomerStatValueText}"/>
                    <TextBlock Text="{Binding CustomerPrintSummary}" Style="{StaticResource CustomerDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CustomerStatCard}">
                <StackPanel>
                    <TextBlock Text="Contact Path" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding CustomerContactSummary}" Style="{StaticResource CustomerStatValueText}"/>
                    <TextBlock Text="Phone, mobile, and email context for the selected customer" Style="{StaticResource CustomerDetailText}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CustomerStatCard}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Selected Customer" Style="{StaticResource LabelTextBlock}"/>
                    <TextBlock Text="{Binding SelectedCustomer.Company, TargetNullValue=No customer selected}" Style="{StaticResource CustomerStatValueText}"/>
                    <TextBlock Text="{Binding CustomerOperationsSummary}" Style="{StaticResource CustomerDetailText}"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.65*" MinWidth="0"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid MinWidth="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel LastChildFill="False">
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="Customer Directory" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Select the exact company or contact before copying, printing, editing, or opening details." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding CustomerResultsSummary}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="12,0,0,0"/>
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource DesktopPaneSubheader}">
                        <DockPanel LastChildFill="True">
                            <WrapPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="Find" Style="{StaticResource LabelTextBlock}" VerticalAlignment="Center" Margin="0,0,6,6"/>
                                <pages:SearchBar Width="300"
                                                 MinWidth="220"
                                                 IsEnabled="{Binding IsCustomerDirectoryActionAvailable}"
                                                 Text="{Binding CustomerSearchTerm, UpdateSourceTrigger=PropertyChanged}"
                                                 SearchCommand="{Binding SearchCustomersCommand}"
                                                 ClearCommand="{Binding ClearCustomerSearchCommand}"
                                                 SearchLabel="Search"
                                                 Margin="0,0,8,6"/>
                            </WrapPanel>
                            <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,0,0">
                                <TextBlock Text="{Binding CustomerFilterStatus}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,8,6"/>
                                <Button Style="{StaticResource PrimaryButton}" Content="Add Customer" Command="{Binding AddCustomerCommand}" IsEnabled="{Binding IsCustomerDirectoryActionAvailable}" Margin="0,0,4,6"/>
                                <Button Style="{StaticResource GhostButton}" Content="Clear" Command="{Binding ClearCustomerSearchCommand}" IsEnabled="{Binding IsCustomerDirectoryActionAvailable}" Margin="0,0,4,6"/>
                                <Button Style="{StaticResource GhostButton}" Content="Print Directory" Command="{Binding PrintCustomerDirectoryCommand}" IsEnabled="{Binding IsCustomerDirectoryPrintAvailable}" Margin="0,0,0,6"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>
                    <DataGrid x:Name="CustomerDataGrid"
                              Grid.Row="2"
                              ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer}"
                              IsReadOnly="True"
                              AutoGenerateColumns="False"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              CanUserAddRows="False"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ContextMenuOpening="CustomerDataGrid_ContextMenuOpening"
                              Style="{StaticResource VirtualizedDataGridStyle}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <EventSetter Event="MouseDoubleClick" Handler="CustomerRow_MouseDoubleClick"/>
                                <EventSetter Event="PreviewMouseRightButtonDown" Handler="CustomerRow_PreviewMouseRightButtonDown"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Company" Width="2.2*" MinWidth="170">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,3">
                                            <TextBlock Text="{Binding Company}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Address, TargetNullValue=No address recorded}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Contact" Width="1.7*" MinWidth="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,3">
                                            <TextBlock Text="{Binding Contact, TargetNullValue=No contact recorded}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Email, TargetNullValue=No email recorded}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Phone" Width="150" MinWidth="125">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,3">
                                            <TextBlock Text="{Binding Phone, TargetNullValue=No phone recorded}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Mobile, TargetNullValue=No mobile recorded}" Style="{StaticResource CaptionTextBlock}" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="1.8*" MinWidth="150"/>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu Style="{StaticResource {x:Type ContextMenu}}" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Open Customer Details" Command="{Binding OpenCustomerDetailsCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}"/>
                                <MenuItem Header="Edit Customer" Command="{Binding EditCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}"/>
                                <MenuItem Header="Copy Contact Handoff" Command="{Binding CopySelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}"/>
                                <MenuItem Header="Print Customer Sheet" Command="{Binding PrintSelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}"/>
                                <Separator/>
                                <MenuItem Header="Print Customer Directory" Command="{Binding PrintCustomerDirectoryCommand}" IsEnabled="{Binding IsCustomerDirectoryPrintAvailable}"/>
                                <MenuItem Header="Delete Customer" Command="{Binding DeleteCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                    <Border Grid.Row="2" MaxWidth="330" MinHeight="120" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsCustomerEmptyStateVisible, Converter={StaticResource BoolToVis}}" Style="{StaticResource DesktopNoteCard}">
                        <StackPanel>
                            <TextBlock Text="No customers found" Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding CustomerEmptyStateMessage}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" MaxWidth="360" MinHeight="120" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource DesktopNoteCard}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCustomerDirectoryBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <ProgressBar IsIndeterminate="True" Height="6" Margin="0,0,0,10"/>
                            <TextBlock Text="Updating customer directory" Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding CustomerFilterStatus}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrushBase}"/>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0" MinWidth="0">
                <Grid MinWidth="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Style="{StaticResource DesktopPaneHeader}">
                        <DockPanel LastChildFill="False">
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock Text="Advisor Handoff" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <TextBlock Text="Selected customer context for service calls, rental documents, and directory printouts." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="14">
                            <TextBlock Text="{Binding SelectedCustomer.Company, TargetNullValue='No customer selected', FallbackValue='No customer selected'}"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding CustomerContactSummary}" Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap" Margin="0,2,0,10"/>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Contact Path" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding CustomerContactSummary}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,2"/>
                                    <TextBlock Text="Use copy contact when handing this customer to rentals, dispatch, or service." Style="{StaticResource CaptionTextBlock}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Address" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding CustomerAddressSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Operational Next Step" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding CustomerOperationsSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <WrapPanel Margin="0,0,0,8">
                                <Button Style="{StaticResource PrimaryButton}" Content="Details" Command="{Binding OpenCustomerDetailsCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                                <Button Style="{StaticResource PrimaryButton}" Content="Edit" Command="{Binding EditCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                                <Button Style="{StaticResource GhostButton}" Content="Copy" Command="{Binding CopySelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                                <Button Style="{StaticResource GhostButton}" Content="Print" Command="{Binding PrintSelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,0,6"/>
                            </WrapPanel>

                            <Border Style="{StaticResource DesktopNoteCard}" Margin="0,4,0,0">
                                <StackPanel>
                                    <TextBlock Text="Desk Checklist" Style="{StaticResource LabelTextBlock}" Margin="0,0,0,4"/>
                                    <TextBlock Text="Confirm the company, verify the best phone or email, copy or print the handoff, then move into the rental or service workflow with the right customer selected." TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                    <Border Grid.Row="2" Style="{StaticResource DesktopSectionActionStrip}">
                        <WrapPanel HorizontalAlignment="Right">
                            <Button Style="{StaticResource PrimaryButton}" Content="Copy Contact" Command="{Binding CopySelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,6,6"/>
                            <Button Style="{StaticResource GhostButton}" Content="Print Sheet" Command="{Binding PrintSelectedCustomerCommand}" IsEnabled="{Binding IsSelectedCustomerActionAvailable}" Margin="0,0,0,6"/>
                        </WrapPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource ToolbarCard}" Margin="0,6,0,0">
            <DockPanel LastChildFill="True">
                <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Style="{StaticResource GhostButton}" Content="Clear Search" Command="{Binding ClearCustomerSearchCommand}" IsEnabled="{Binding IsCustomerDirectoryActionAvailable}" Margin="0,0,0,6"/>
                </WrapPanel>
                <TextBlock Text="{Binding SelectedCustomerSummary}" Style="{StaticResource CaptionTextBlock}" VerticalAlignment="Center" Margin="2,0,10,0" TextWrapping="Wrap"/>
            </DockPanel>
        </Border>
    </Grid>
</Page>