<Page x:Class="InventoryManagementApp.Views.Pages.MonthlyTargetsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Monthly Targets">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Financial Year" Style="{DynamicResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding FinancialYears}"
                          SelectedItem="{Binding SelectedFinancialYear}"
                          DisplayMemberPath="DisplayName"
                          Width="200"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Financial Year Start" Style="{DynamicResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding StartMonths}"
                          SelectedItem="{Binding SelectedStartMonth}"
                          DisplayMemberPath="DisplayName"
                          Width="200"/>
            </StackPanel>
        </StackPanel>
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrushBase}"
                BorderThickness="1"
                Padding="12"
                CornerRadius="8">
            <TextBlock Text="Enter the monthly targets for the selected financial year."
                       Style="{DynamicResource CaptionTextBlock}"/>
        </Border>
        <DataGrid Grid.Row="2"
                  Margin="0,16,0,16"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding MonthlyTargets}"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  Style="{DynamicResource VirtualizedDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Month"
                                    Binding="{Binding MonthDisplay}"
                                    IsReadOnly="True"
                                    Width="2*"/>
                <DataGridTemplateColumn Header="Target"
                                        Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding TargetAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     HorizontalContentAlignment="Right"
                                     Margin="0"
                                     Style="{DynamicResource {x:Type TextBox}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="{Binding StatusMessage}" Style="{DynamicResource CaptionTextBlock}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Save Targets"
                    Command="{Binding SaveTargetsCommand}"
                    Style="{DynamicResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Page>
