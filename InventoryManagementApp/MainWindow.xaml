<!-- MainWindow.xaml -->
<Window x:Class="InventoryManagementApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:InventoryManagementApp.Controls"
        xmlns:pages="clr-namespace:InventoryManagementApp.Views.Pages"
        Title="{Binding WindowTitle}" Width="1280" Height="800" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="214"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrushBase}"
                BorderThickness="0,0,0,1"
                Padding="8,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding WindowTitle}" Style="{StaticResource HeadingTextBlock}"/>
                    <TextBlock Text="Workshop inventory and rental control" Style="{StaticResource CaptionTextBlock}"/>
                </StackPanel>
                <pages:SearchBar Grid.Column="1"
                                 Width="460"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center"
                                 Margin="10,0,0,0"
                                 Text="{Binding GlobalSearchText}"
                                 SearchCommand="{Binding GlobalSearchCommand}"
                                 SearchLabel=""/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Border Width="26" Height="26" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrushAlt}" BorderThickness="1" Margin="0,0,6,0">
                        <controls:UserAvatar UserName="{Binding CurrentUserName}"
                                              UserPhotoPath="{Binding CurrentUserPhotoPath}"
                                              Foreground="{Binding CurrentUserInitialsBrush}"
                                              FontSize="13"/>
                    </Border>
                    <StackPanel Margin="0,0,8,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUserName}" FontWeight="SemiBold" TextWrapping="NoWrap"/>
                        <TextBlock Text="{Binding CurrentUserRole}" Style="{StaticResource CaptionTextBlock}" TextWrapping="NoWrap"/>
                    </StackPanel>
                    <Button Style="{StaticResource GhostButton}" Content="Switch User" Command="{Binding SwitchUserCommand}" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource GhostButton}" Content="Exit" Command="{Binding ExitCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.ColumnSpan="2"
                Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrushBase}"
                BorderThickness="0,0,0,1"
                Padding="6,3">
            <WrapPanel Orientation="Horizontal">
                <ToggleButton Style="{StaticResource SectionToggleButton}" Content="Overview" Command="{Binding SelectOverviewSectionCommand}" IsChecked="{Binding IsOverviewSelected, Mode=OneWay}"/>
                <ToggleButton Style="{StaticResource SectionToggleButton}" Content="Operations" Command="{Binding SelectOperationsSectionCommand}" IsChecked="{Binding IsOperationsSelected, Mode=OneWay}"/>
                <ToggleButton Style="{StaticResource SectionToggleButton}" Content="Insights" Command="{Binding SelectInsightsSectionCommand}" IsChecked="{Binding IsInsightsSelected, Mode=OneWay}"/>
                <ToggleButton Style="{StaticResource SectionToggleButton}" Content="Data" Command="{Binding SelectDataSectionCommand}" IsChecked="{Binding IsDataSelected, Mode=OneWay}" Visibility="{Binding IsDataSectionVisible, Converter={StaticResource BoolToVis}}"/>
                <ToggleButton Style="{StaticResource SectionToggleButton}" Content="Admin" Command="{Binding SelectAdminSectionCommand}" IsChecked="{Binding IsAdminSelected, Mode=OneWay}" Visibility="{Binding IsAdminSectionVisible, Converter={StaticResource BoolToVis}}"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Grid.Column="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrushBase}"
                BorderThickness="0,0,1,0"
                Padding="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="{DynamicResource SurfaceAltBrush}"
                        BorderBrush="{DynamicResource BorderBrushAlt}"
                        BorderThickness="1"
                        Padding="6"
                        Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding CurrentNavSectionIconGlyph}"
                                   Width="28"
                                   Height="28"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="18"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource ForegroundMutedBrush}"/>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Sections" Style="{StaticResource PageTitleTextBlock}"/>
                            <TextBlock Text="{Binding CurrentNavSectionTitle}" Style="{StaticResource CaptionTextBlock}"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <ScrollViewer Grid.Row="1" PreviewMouseWheel="LeftNavScrollViewer_PreviewMouseWheel">
                    <ItemsControl ItemsSource="{Binding CurrentNavItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource NavButton}" Command="{Binding Command}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="28"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding DisplayNumber, StringFormat={}{0:00}}"
                                                   Foreground="{DynamicResource ForegroundMutedBrush}"
                                                   TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Title}"
                                                   TextWrapping="NoWrap"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <Frame Grid.Row="2"
               Grid.Column="1"
               Name="MainFrame"
               NavigationUIVisibility="Hidden"
               JournalOwnership="OwnsJournal"
               Background="{DynamicResource BackgroundBrush}"
               Content="{Binding CurrentPage}"
               Margin="{StaticResource PagePadding}"/>
    </Grid>
</Window>
